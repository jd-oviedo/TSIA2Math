node_modules/
.next/
tsconfig.tsbuildinfo
next-env.d.ts
.env.local

# Sentry Config File
.env.sentry-build-plugin
migrate_math.py
migrate_fractions.py
fix_currency.py
upload_to_supabase.py
public/data/question_bank.backup.json
public/data/question_bank.backup2.json

# Python
__pycache__/
*.pyc

# Local design reference screenshots, not part of the shipped app
reference/

# Local-only prod snapshots (rollback path for restore_bank_text.py)
scratchpad/

# Playwright probe artefacts. The verify_*/shoot_* scripts write a temporary
# route under app/um-probe-* before their build and delete it on exit; if a run
# is killed mid-flight the directory survives and must never be committed. Their
# screenshots land in scratch-shots-*/ for the same reason.
app/um-probe-*/
scratch-shots-*/
