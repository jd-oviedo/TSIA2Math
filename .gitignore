node_modules/
.next/
tsconfig.tsbuildinfo
next-env.d.ts
.env.local

# Sentry Config File
.env.sentry-build-plugin
migrate_math.py
migrate_fractions.py
fix_currency.py
upload_to_supabase.py
public/data/question_bank.backup.json
public/data/question_bank.backup2.json

# Python
__pycache__/
*.pyc

# Local design reference screenshots, not part of the shipped app
reference/

# Local-only prod snapshots (rollback path for restore_bank_text.py)
scratchpad/

# Before/after row dumps written by curriculum/migrations/upload_curriculum.py.
# Production content including answer keys, and one pair per upload, so they are
# kept local rather than committed. See deferred-updated-at-audit-trail.md.
curriculum/migrations/snapshots/
