# -*- coding: utf-8 -*-
"""
Ordered (regex -> slug) rules for scripts/tag_misconceptions.py.

Authored by reading all 3,348 distractor strings. Topic rules run first, then
strand rules, then global rules; first match wins. No default -- an unmatched
distractor is a build failure.

Every slug here must exist in data/docs/misconception_taxonomy.json.
"""

TOPIC_RULES = {
# ---------------------------------------------------------------- QR unit 1
"QR.1.1":[
 (r"halv\w+ the radicand|square root as division by two|as division by 2", "halves_the_radicand"),
 (r"squares? the radicand|reversing the operation", "squares_instead_of_roots"),
 (r"greatest to least|least to greatest|reverses the (order|requested)|reversing the requested|listing from", "order_direction_reversed"),
 (r"negative sign flips|forgets that the negative|most negative", "negative_reverses_radical_order"),
 (r"adding rather than multiplying the radicands|replaces .{0,20}with .{0,12}dropping the radical", "false_radical_distribution"),
 (r"largest coefficient|only the number shown under the radical|because .{0,6} looks large", "radical_coefficient_size_as_value"),
 (r"dropping the radical", "radical_dropped_for_coefficient"),
 (r"irrational .{0,40}(larger|greater|exceed)|assumes irrational|automatically larger|must be smaller than a nearby whole|avoids the radicals|misjudges it as larger", "irrational_assumed_larger"),
 (r"places the value near the radicand|choosing whole numbers on either side|near the radicand itself", "radicand_mistaken_for_value"),
 (r"perfect squares around|bracket|between .{0,4} and", "wrong_perfect_square_bracket"),
 (r"misconverts?|misreads .{0,20}as|inverting or misapplying", "fraction_digit_gluing"),
 (r"rounds .{0,30}(down|up)|approximation|estimates?|under-?estimat|over-?estimat|swapping the two largest|swaps", "closer_endpoint_error"),
 (r"computes the difference correctly but reverses which path", "order_direction_reversed"),
 (r"concludes the paths are equal|declares the two equal", "rounding_hides_difference"),
],
"QR.1.2":[
 (r"greatest to least|least to greatest|slowest to fastest|reversing the requested|inverted the meaning", "order_direction_reversed"),
 (r"assumes the values are already in order|as written", "values_assumed_preordered"),
 (r"more than half|'?more than half'?|reasons that .{0,30}out of", "benchmark_judged_from_digits"),
 (r"largest because|biggest number shown|whole number", "percent_sign_confusion"),
 (r"writes the digits|digits .{0,20}as|reads '?\S+'? as the number|treating it as about", "fraction_digit_gluing"),
 (r"flips the fraction|inverting|by flipping the division", "reversed_division"),
 (r"rounds .{0,40}(up|down)|rounding artifact|concludes the two are equal|cannot tell it apart|conclude", "rounding_hides_difference"),
 (r"negative sign flips|more negative", "negative_reverses_radical_order"),
 (r"does not convert|already written as a decimal|cannot be compared", "refuses_to_compare_forms"),
 (r"three decimal places|decimal places", "longer_decimal_is_larger"),
 (r"ignoring that it is (above|below)|does not check the upper bound|includes|underestimates|overestimates|miscomput", "closer_endpoint_error"),
 (r"gap of|miscomputes the gap|reverses which share", "subtracts_in_wrong_order"),
],
"QR.1.3":[
 (r"999|three-digit period|two-digit period|repeating-block method|pure-repeating rule", "repeating_block_denominator_wrong"),
 (r"not the repeating block|repeating block as", "repeating_block_misidentified"),
 (r"stops the long division|after one step|before the repeating pattern", "long_division_stopped_early"),
 (r"truncat|chops|stops the division after two|rounds the repeating|losing the repeating|ignoring the repeating|treating it as a terminating|treating the decimal as if it terminates", "truncates_repeating_decimal"),
 (r"repeats because|terminates|factoring it into|is not a factor of any power", "terminating_test_confusion"),
 (r"concatenates|digits .{0,20}side by side|reads the digits|reverses the digits|writes the digits", "fraction_digit_gluing"),
 (r"inverts the fraction|as the denominator|as the numerator|numerator and denominator", "numerator_denominator_swap"),
 (r"only the numerator|ignoring the denominator", "numerator_as_percent"),
 (r"tenths|hundredths|place value|misplaces the decimal|dropping the last digit|factor of 10", "place_value_slip"),
 (r"does not simplify|unsimplified|fully reduce|simplifies incorrectly", "stops_before_simplifying"),
 (r"assumes .{0,30}must differ|without simplifying or dividing|compares numerators and denominators", "refuses_to_compare_forms"),
 (r"rounding the decimal before converting|rounds to|estimates|approximates", "rounds_too_early"),
],
"QR.1.4":[
 (r"halv\w+ the radicand|instead of estimating (its|the) square root", "halves_the_radicand"),
 (r"near the radicand itself|integers on either side of", "radicand_mistaken_for_value"),
 (r"midpoint", "midpoint_used_as_estimate"),
 (r"without squaring|does not verify by squaring|without examining|without testing whether|checks only that", "estimate_not_verified_by_squaring"),
 (r"without refining|does not refine|lower bounding integer|upper bounding integer|rounds (up|down|to)", "rounds_to_bounding_integer"),
 (r"format of the estimates|decimal place|nonsensical reasoning|distance from perfect square", "accuracy_judged_by_format"),
 (r"perfect squares|bounding squares|brackets|undershoots|anchor", "wrong_perfect_square_bracket"),
 (r"closer to|nearest multiple|which bound", "closer_endpoint_error"),
 (r"reverses the inequality|concluding", "estimate_not_verified_by_squaring"),
 (r"confuses area with perimeter", "perimeter_area_confusion"),
 (r"divides the area by", "divides_instead_of_multiplies"),
 (r"overestimates|underestimates|misestimates|treating it as", "closer_endpoint_error"),
],
"QR.1.5":[
 (r"conjugate to the numerator|does not apply the same conjugate|not distribute the conjugate|does not distribute the conjugate", "conjugate_applied_to_denominator_only"),
 (r"instead of the conjugate|original denominator rather than its conjugate", "multiplies_by_denominator_not_conjugate"),
 (r"cancel .{0,30}from numerator and denominator|as if .{0,20}treating the denominator as a factor", "radical_cancelled_as_factor"),
 (r"loses track of the term|coefficient .{0,20}\(loses|treats .{0,20}as having coefficient", "coefficient_lost_in_multistep"),
 (r"under one radical|inside a single radical|cannot be merged", "rational_and_irrational_merged"),
 (r"false rule|√a|subtracts radicands|adds the radicals|misreads the minus sign as a plus", "false_radical_distribution"),
 (r"square root with halving|treats .{0,12}as .{0,12}\(confuses taking a square root", "halves_the_radicand"),
 (r"skips rationalization|whole-number approximations|rounds .{0,20}down to the nearest perfect", "rounds_too_early"),
 (r"ignores all negative signs|ignores the negative sign|does not apply the negative|does not recognize that subtracting a negative|believing that a result involving two negatives", "drops_negative_sign"),
 (r"applies a negative sign|adds .{0,20}instead of|confused by the negative context", "sign_error_on_constant"),
 (r"reverses the order of subtraction", "subtracts_in_wrong_order"),
 (r"misaligns the decimal|arithmetic slip converting to a mixed number", "place_value_slip"),
 (r"forgetting to multiply the numerator by the same factor", "numerators_not_rescaled"),
 (r"drops the whole-number portion|forgets to add|stops there, forgetting|forgets the", "answers_intermediate_value"),
 (r"ignores the coefficient", "coefficient_lost_in_multistep"),
],
"QR.1.6":[
 (r"nearest (hundredth|whole number|ten\b|thousand|tenth|thousandth|dollar|cent)|rounds all the way|instead of the nearest", "rounds_to_wrong_place"),
 (r"thousandths digit .{0,20}instead of|wrong reference digit|looks at the (thousandths|ones) digit|reference digit", "wrong_reference_digit"),
 (r"rounds down despite|defaults to rounding down|uncertain whether", "rounds_down_despite_five_or_more"),
 (r"increment the rightmost|bumps the", "rounding_as_increment_rightmost"),
 (r"carry|propagates", "carry_not_propagated"),
 (r"two-step|direct rounding|extra round-up|already-rounded", "two_step_rounding_assumed_equal"),
 (r"rounds to .{0,30}because the tenths digit|integer part is too small|close enough", "rounds_to_wrong_place"),
 (r"reverses the outputs", "two_step_rounding_assumed_equal"),
],
"QR.1.7":[
 (r"squaring with doubling|confuses squaring|as .{0,10}instead of .{0,10}:|treats .{0,6}² as", "squaring_confused_with_doubling"),
 (r"ignores the (parentheses|grouping)|groups incorrectly|drops the parentheses", "drops_grouping_symbols"),
 (r"ignores the division|ignores the exponent entirely|evaluates only", "operation_ignored_entirely"),
 (r"forgets to subtract|subtracts only", "answers_intermediate_value"),
 (r"adds before multiplying|left to right|adds first|addition before multiplication|multiplication priority|after the exponent|violating the order", "order_of_operations_violated"),
],
"QR.1.8":[
 (r"absolute value leaves the sign|sign unchanged", "absolute_value_leaves_sign"),
 (r"absolute value with squaring|applies the absolute value before the exponent|treats .{0,8}² as", "absolute_value_as_squaring"),
 (r"added to itself to reach zero", "absolute_value_as_additive_inverse"),
 (r"confusing absolute value of a difference with a product|multiplies the two values inside", "absolute_difference_as_product"),
 (r"only the positive case|misses the negative case|considers only the (upper|positive)", "absolute_equation_one_case_only"),
 (r"reverses the inequality direction|solution for", "absolute_inequality_direction_reversed"),
 (r"only the boundary points|strictly between", "absolute_inequality_boundary_only"),
 (r"forgets to (apply|take) the absolute value|ignores both absolute values|ignores the absolute value|applies absolute value only", "absolute_value_leaves_sign"),
 (r"adds instead of subtracts|strips all negative signs and adds|treating subtraction as addition", "adds_instead_of_subtracts"),
 (r"sign error|writes x = ", "sign_error_on_constant"),
 (r"wrong direction for both cases", "subtracts_in_wrong_order"),
 (r"considers only", "absolute_equation_one_case_only"),
],
}

TOPIC_RULES.update({
# ---------------------------------------------------------------- QR unit 2
"QR.2.1":[
 (r"multiplies the two ratio numbers|multiplies both ratio numbers", "ratio_terms_multiplied"),
 (r"selects (boys|green|red|white)|scales by .{0,20}instead of|multiplies by .{0,12}instead of .{0,12}using the|wrong ratio number|scaling correctly but selects", "scales_by_wrong_ratio_term"),
 (r"total number of ratio parts|counts the total number of ratio parts|sums the ratio parts|counts the total", "ratio_parts_reported_in_answer_units"),
 (r"reads the \d in the ratio|ratio .{0,10}as the (fraction|answer)|treats the ratio", "ratio_term_as_value"),
 (r"part-to-part|3/2 of|exceeds the entire", "part_whole_confusion"),
 (r"divides .{0,40}by the wrong ratio number|by the (green|blue|girls'?) part|rather than by the \d+ total", "uses_wrong_total"),
 (r"adds the second ratio number|adds dogs and cats|instead of scaling by ratio units", "adds_instead_of_scales"),
 (r"stops there|stops,|but does not subtract|reports it as the answer|reports the total|forgetting to apply|without first finding the unit", "answers_intermediate_value"),
 (r"subtracts the original flour", "adds_instead_of_scales"),
 (r"multiplies the real-world distance .{0,30}instead of dividing|divides by .{0,10}instead of multiplying|inverting the scale", "inverts_conversion_direction"),
 (r"ignoring that the blend|applying the most expensive|uses only|ignoring", "omits_second_component"),
 (r"uses \S+ g for both", "omits_second_component"),
],
"QR.2.2":[
 (r"skipping the proportional division|without dividing by the pole shadow", "proportional_division_step_skipped"),
 (r"head start|closing speed|relative|catch-up|still producing|Machine (A|B)'s rate|full speed rather than", "relative_speed_ignored"),
 (r"alligation", "alligation_ratio_misapplied"),
 (r"inverts the rate|inverts the ratio|shadow/height", "reversed_division"),
 (r"stops there|reports the rate itself|reports .{0,25}as the answer|without multiplying by|without first computing|reports the cost as the answer|but stops", "answers_intermediate_value"),
 (r"multiplies the target|multiplies the original|multiplies the pole height|multiplies both numbers|multiplies the rate numerator", "multiplies_instead_of_divides"),
 (r"divides the target pages by the given time|divides \S+ by", "reversed_division"),
 (r"adds the original job count|instead of using the proportional", "adds_instead_of_scales"),
 (r"reports the tree's shadow|without performing any calculation|reports the original time", "answers_intermediate_value"),
 (r"subtracts the original cost|omits the existing salt|omits the salt", "omits_second_component"),
 (r"rounds the unit prices|rounds to", "rounds_too_early"),
 (r"adds that time to Train A", "relative_speed_ignored"),
],
"QR.2.3":[
 (r"returns to the original|markup .{0,40}discount|discount applies to the higher", "markup_discount_assumed_to_cancel"),
 (r"tip on the wrong base|applies the tax to the original|post-tax|omitting the tax|only the .{0,12}tip", "percent_applied_to_wrong_stage_base"),
 (r"improvement goal|read '?.{0,20}improvement", "goal_threshold_misread"),
 (r"by the new (price|population|score|value)|change by the new", "percent_change_wrong_base"),
 (r"ratio of the new value|new value to the original|percent remaining|ratio of prices", "new_over_original_as_change"),
 (r"dollar amount of the change|raw point change|absolute", "absolute_change_as_percent"),
 (r"adds the two percent|subtracts the two percent|additive rather than multiplicative|adding both rates|combined multiplier|truncates", "percent_changes_added"),
 (r"inverts the (division|ratio)|dividing the original by the new", "reversed_division"),
 (r"subtracts the percent from the whole|adds the percent to the whole|subtracts the part from the whole", "percent_sign_confusion"),
 (r"multiplies instead of divides", "multiplies_instead_of_divides"),
 (r"confuses \d+% with|wrong percent-to-fraction|reciprocal of the wrong fraction|doubling the percent", "percent_sign_confusion"),
 (r"stops before applying|correctly applies", "answers_intermediate_value"),
 (r"subtracts \d+% of the new salary|adds \d+% of the new salary|estimates the original", "percent_applied_forward_not_reversed"),
],
"QR.2.4":[
 (r"another \d+% (increase|discount|markup)|applies another|moving further from the original|applying the change forward|applies the tax rate to the wrong base|adds .{0,20}to the sale price|adds \d+% of the total", "percent_applied_forward_not_reversed"),
 (r"reports \S+ liters instead of the added water|volume of acid|water to add|treating the percent difference as a volume", "mixture_reports_total_not_added"),
 (r"break-even|offsets|coupon multiplier|compound reasoning|misapplying compound", "breakeven_markup_assumed_equal_to_discount"),
 (r"adds the two markup percents|subtracts the two percent|adds the percent number directly|subtracts the tax rate as a whole number|subtracts the percent value directly|as dollars", "percent_changes_added"),
 (r"finds \d+% of the given result|rather than setting up an equation|applies the \d+% rate to the commission|adds the commission rate", "percent_applied_forward_not_reversed"),
 (r"wrong base", "percent_change_wrong_base"),
 (r"stops there|reports it as the answer|without dividing|without setting up an equation|reports the number of students", "answers_intermediate_value"),
 (r"estimates \S+ ≈|instead of using the exact divisor", "rounds_too_early"),
 (r"ignores the base salary|divides the full|in the wrong order", "omits_constant_term"),
 (r"uses the percent who passed", "part_whole_confusion"),
 (r"only the first \d+% markup|ignoring the second", "omits_second_component"),
],
"QR.2.5":[
 (r"averages the two segment speeds|averages the two|averaging", "speeds_averaged_arithmetically"),
 (r"adds the two individual times|averages the two individual times|sequentially rather than simultaneously", "times_added_instead_of_rates_combined"),
 (r"only the (second|first) segment|uses Pipe A's rate for both|splits the total distance equally|splits the", "single_segment_used_for_whole"),
 (r"closing rate value|confusing a speed with a distance", "closing_rate_reported_as_distance"),
 (r"inverts the (division|operation)|quantity ÷ price|pounds per dollar|divides the rate by the quantity", "reversed_division"),
 (r"subtracts the quantity from the price|adds the rate and quantity|adds the total boxes", "adds_instead_of_scales"),
 (r"multiplies price by quantity|multiplies the given values", "multiplies_instead_of_divides"),
 (r"stops there|without multiplying|reports .{0,25}as the answer|without comparing|without performing any unit conversion|but stops", "answers_intermediate_value"),
 (r"swapped rate|assigns Brand B's unit rate", "reads_wrong_category"),
 (r"treats milliliters as liters|instead of \d+ \(confusing the conversion|instead of \d+ \(applying|divides \S+ mL by|by \d+ instead of \d+", "inverts_conversion_direction"),
 (r"multiplies \S+ by \S+ to eliminate|without applying the miles-to-feet|uses \d+ minutes instead of", "inverts_conversion_direction"),
 (r"dropping the last gallon|uses \S+ gallons instead", "wrong_period_count_used"),
 (r"multiplies by Train B's speed", "reads_wrong_category"),
 (r"guesses or estimates|without using the time constraint|another incorrect shortcut|divides by the number of page types", "proportion_solved_for_wrong_unknown"),
],
"QR.2.6":[
 (r"only the whole number|drops the decimal|whole number portion|ignoring the \S+ (foot|lb|gallon)|fractional part", "converts_whole_number_part_only"),
 (r"stopping one step too early|does not complete the final|converts only to quarts|converts to pints", "conversion_stopped_one_step_early"),
 (r"forgets to add the final endpoint|endpoint post", "fencepost_error"),
 (r"adds the fill rate to the drain rate|treating both as draining|net", "rates_added_instead_of_netted"),
 (r"ignores the fill pipe|uses it alone|ignoring the drain", "omits_second_component"),
 (r"converting in the wrong direction|divides instead of multiplies|multiplies by \S+ instead of dividing|off by a factor|divides by \d+ instead of|multiplies by \d+ instead of", "inverts_conversion_direction"),
 (r"adds the conversion factor", "adds_instead_of_scales"),
 (r"copies the number|without applying any conversion|treating different units as directly comparable|without converting", "refuses_to_compare_forms"),
 (r"identifies the wrong runner|assuming both runners ran", "misreads_direction_of_change"),
 (r"without first calculating total gallons|without multiplying by the|reports that number as fluid ounces|correctly finds", "answers_intermediate_value"),
 (r"halving|uses \S+ feet as the spacing", "wrong_period_count_used"),
 (r"forgets to divide by \d+ to convert", "conversion_stopped_one_step_early"),
],
"QR.2.7":[
 (r"squared|area units require|linear conversion factor .{0,20}to the area|divides by the squared factor|divides by \S+ once more", "area_conversion_factor_not_squared"),
 (r"forgets to add the \S+-degree offset|only adds \d+ to the Celsius|skipping the required offset|without first subtracting|forgets to multiply by", "temperature_offset_omitted"),
 (r"rounds the conversion factor|rounds both values aggressively|overestimates the conversion|losing precision", "conversion_factor_rounded"),
 (r"converting in the wrong direction|divides instead of multiplies|multiplies instead of divides|inverts the conversion|applies the Celsius-to-Fahrenheit formula instead of its inverse|uses \S+ instead of", "inverts_conversion_direction"),
 (r"adds the conversion factor|subtracts the conversion factor", "adds_instead_of_scales"),
 (r"without converting|treating km/h and mph as equivalent|leaving the two cars in incomparable|without converting the US car", "refuses_to_compare_forms"),
 (r"assigns the win to Car A|subtracts Car B's original", "misreads_direction_of_change"),
 (r"rounds down to \S+ bags|reports the converted weight|confusing units", "answers_intermediate_value"),
 (r"declares a tie", "rounding_hides_difference"),
 (r"dropping a factor", "place_value_slip"),
],
"QR.2.8":[
 (r"constant difference|differences are constant|constant ratio y/x, not a constant difference|is linear but not direct variation|constant function|quadratic function", "constant_difference_as_direct_variation"),
 (r"first y-value|identifies the first", "first_value_as_constant_of_variation"),
 (r"inverse relationship|applies it as a multiplier rather than dividing|inverts the batch-size|direct variation in both", "inverse_relationship_treated_as_direct"),
 (r"inverts the ratio|x/y", "reversed_division"),
 (r"adds the change in x|adds the given force|treating the relationship as additive|instead of using the proportional", "adds_instead_of_scales"),
 (r"multiplies the known y-value|multiplies the given force|skipping the step of finding k", "adds_instead_of_scales"),
 (r"divides y by \d+ instead of|uses the original batch size", "uses_wrong_total"),
 (r"new value of y as a percent|as a percent of the original", "new_over_original_as_change"),
 (r"divides the percent change|compound percent|compounding on itself", "percent_changes_added"),
],
})

TOPIC_RULES.update({
# ---------------------------------------------------------------- QR unit 3
"QR.3.1":[
 (r"merges the flat fee|charging \$?\S+ per mile|multiplies the flat fee|treats all three dollar amounts as per-hour|attaches the per-hour label|includes only one of the two fixed fees", "flat_fee_merged_into_rate"),
 (r"reverses the (subtraction|division) order|orders the terms as they appear|writing the numbers in the order|reverses the subtraction", "less_than_order_reversed"),
 (r"drops the (parentheses|grouping)|incorrectly grouping|groups the .{0,20}with the tripling|pulls the .{0,10}inside|divides only the", "drops_grouping_symbols"),
 (r"confuses '?(more|less|product|twice|divided by|increased by|decreased by|sum)'?|interprets '?(more|twice|less)'?|reads '?(more|less|sum|twice)'?", "keyword_mistranslated"),
 (r"raises w to the second power", "keyword_mistranslated"),
 (r"multiplies only|fails to (double|multiply)|doubles only the first term|leaves p untouched|not the full quantity", "partial_distribution"),
 (r"swaps the roles", "slope_intercept_swap"),
 (r"sign error on the constant|subtracting \S+ instead of adding", "sign_error_on_constant"),
 (r"wrong quantity|the wrong amount", "wrong_quantity_grouped"),
],
"QR.3.2":[
 (r"adds the coefficient and the constant|adds the entry fee and the hourly|adds the fill rate and the starting|reports the sum as the (rate|per-unit)|treating the sum as the rate|compares the sums", "coefficient_plus_constant_as_rate"),
 (r"selects the (entire variable term|full term|full expression|full total-cost)|selects the variable x itself|rather than isolating", "variable_term_read_as_rate"),
 (r"selects the constant|constant term|starting value, not the rate|starting (cost|amount|balance|height)|one-time entry fee|reports the (first y-value|final value|starting)|fixed total|steady starting|shared constant|compares the constant|larger constant|the constant is not the rate|repeating the error of treating the constant|largest starting|subtracts the constants", "constant_read_as_rate"),
 (r"per-text rate|covers a dozen|bulk pricing|per-page rate .{0,20}directly to reams|pages-per-ream|fixed add-on|daily figure|common unit|spread across", "rate_not_normalised_to_unit"),
 (r"depends on the number of texts|assumes all three rates are equal|because both expressions are linear|order the expressions are written", "rate_assumed_quantity_dependent"),
 (r"inverts the ratio|change in x by the change in y|change in week by the change in height", "slope_run_over_rise"),
 (r"ignores the sign of the coefficient", "drops_negative_sign"),
 (r"swaps the roles", "slope_intercept_swap"),
 (r"overlooks that gaining|missing that losing|missing that", "constant_read_as_rate"),
],
"QR.3.3":[
 (r"swaps the (prices|wheel counts|weights|two prices)|attaching \S+ to", "prices_swapped_between_quantities"),
 (r"adds the two (prices|wheel counts|per-marble weights) into (a single|one) rate|adds the prices and the quantities", "rates_merged_into_single_rate"),
 (r"counts the (fruit|vehicles|marbles) but (drops|ignores)", "quantities_counted_without_rates"),
 (r"reverses the relationship|treating hats as twice|reads '?twice as many'?|attaches the '?\S+ more'?|fewer", "comparative_relationship_reversed"),
 (r"one-time flat charge|as a one-time", "per_unit_price_as_flat_charge"),
 (r"forgets to add the (hats|cows)|drops the \S+-animal difference|but forgets to add", "omits_second_component"),
 (r"fails to multiply|multiplies only|distributes the \S+ over only|forgets to add \S+W|omits the constant side", "partial_distribution"),
 (r"subtracts the difference instead of adding|adds the fee instead of subtracting|subtracts the \S+ fee twice|subtracts the rate from|adds the rate to", "adds_instead_of_subtracts"),
 (r"treats students and adults as equal|applies the factor of \S+ to the adult price|multiplies the wrong group", "prices_swapped_between_quantities"),
 (r"treats all four sides as length", "irregular_treated_as_rectangle"),
 (r"reuses the total count|sets it equal to the \S+-item difference", "uses_wrong_total"),
 (r"omits the \$?\S+ flat fee entirely|strands the|misreads the material rate|divides the full|multiplies the two numbers instead of dividing", "omits_constant_term"),
],
"QR.3.4":[
 (r"swaps slope and intercept|swaps the roles|starting-value language|per-hour rate language|per-week rate|per-person language|fixed-cost language|per-mile rate language|fixed-charge language|starting-height meaning|making \S+ the (constant|setup fee|hourly rate)|treating the larger starting fee|the \$?\S+ constant with", "slope_intercept_swap"),
 (r"calls the (constant|rate|per-item rate|per-minute rate) the total", "rate_called_the_total"),
 (r"confuses the (rate|starting balance|starting amount) with the variable|calling \S+ a number of (hours|weeks|items|people|seconds|months)|count of free minutes|treating \S+ as the number of seconds|treating \S+ as the initial fuel", "rate_confused_with_variable"),
 (r"ties the \$?\S+ to a specific mileage|the intercept is the value at zero|invents a per-dollar interpretation|again uses per-mile language|mislabeling the fixed charge", "intercept_tied_to_nonzero_input"),
 (r"ignores the negative sign|keeps the rate positive|reading the slope as a (gain|rise)", "drops_negative_sign"),
 (r"subtracts the registration fee instead of adding", "adds_instead_of_subtracts"),
 (r"adds the slope and intercept into one|erases the setup fee", "flat_fee_merged_into_rate"),
 (r"bases a claim about overall cost on the intercept|steeper slope makes|correct interpretation", "intercepts_compared_instead_of_slopes"),
],
"QR.3.5":[
 (r"combines the unlike terms|combines every term as if like|as if like|treats all (three|four)? ?terms as like|adds all four (terms|numbers)|adds every coefficient|merging the variables|combines all three terms|combines the unlike final terms", "combines_unlike_terms"),
 (r"multiplies the (coefficients|x-coefficients|constants)", "multiplies_coefficients"),
 (r"multiplies the variables together", "multiplies_variables"),
 (r"drops the variable|attaches a single variable", "drops_variable"),
 (r"drops the constant|discards the \S+ as if an unlike term|keeps only the larger constant|drops the", "drops_unlike_term"),
 (r"keeps only one negative sign|ignores the negative in front of the parentheses|ignores the minus|treats the second group as|missing the sign change|treats \S+ as \S+, missing|applies the subtraction only to", "drops_negative_on_group"),
 (r"distributes the .{0,20}but not to|brings the inner constants down unmultiplied|forgets to multiply|distributes .{0,10}only to|distributes to the x-terms but", "partial_distribution"),
 (r"adds the x-coefficients instead of subtracting|subtracts the constants instead of adding|adds the coefficients|subtracts the constants", "adds_instead_of_subtracts"),
 (r"ignores the factor of|treats \S+ as \S+ when combining|treats the \S+ as", "ignores_unit_coefficient"),
 (r"computes \S+ as \S+ instead of", "sign_error_on_constant"),
],
"QR.3.6":[
 (r"forgets to divide by the change in|reporting the rise alone|change in (y|temperature|height|subscribers|altitude) \S* ?but forgets|total change in y correctly", "rise_reported_as_rate"),
 (r"reports the change in x|finds the run|larger x-value as the run", "run_reported_as_rate"),
 (r"inverts the (ratio|formula)|dividing run by rise|dividing (days|time|the change in weeks) by", "slope_run_over_rise"),
 (r"drops the negative sign|describing rising altitude|for cooling data", "drops_negative_sign"),
 (r"wrong rows|wrong interval|from day \S+ to day|this interval has|stops at the first|the overall rate from year", "wrong_interval_selected"),
 (r"number of gaps between the listed rows", "row_gaps_counted_as_change"),
 (r"assumes the data is linear|without computing each interval", "constant_rate_assumed"),
 (r"subtracts the y-values in the wrong order", "subtracts_in_wrong_order"),
 (r"adds the rate to the starting value just once|forgetting to add the starting|reports the run itself", "answers_intermediate_value"),
 (r"compares the total change in y", "rise_reported_as_rate"),
],
"QR.3.7":[
 (r"compares the y-intercepts|subtracts the starting values|comparing intercepts|starting cost|starting amounts|starting value|higher starting|starting balances|starting heights", "intercepts_compared_instead_of_slopes"),
 (r"because both (equations|costs) are linear|must change at the same rate|must rise at the same rate|must grow at the same rate|assumes all three", "linearity_assumed_to_imply_equal_rates"),
 (r"cannot be compared|a graph is required|requires (knowing|a graph)|no comparison is possible|not enough information|believes a table and an equation", "comparison_declared_impossible"),
 (r"adds the two (rates|drain rates)|reports (Plan A's|Tank B's|Line \S+'s|N's) (rate|total increase|drain rate) alone|adds the two constants|adds the two total increases", "rates_summed_instead_of_differenced"),
 (r"crossover point|stays equal afterward", "crossover_assumed_permanent_tie"),
 (r"stays ahead", "larger_start_assumed_to_stay_ahead"),
 (r"inverts (each slope|the table's rate|the table)|run over rise|computing g's rate by inverting", "slope_run_over_rise"),
 (r"treats \S+ as greater than|smaller-magnitude slope|misreads the negative slopes|both slopes are negative|both rates are negative|similar-looking fractions|fractional slopes|wrong order, gets a (positive|negative) rate|reverse order", "misreads_direction_of_change"),
 (r"without dividing by the change in x|as Line \S+'s rate without dividing|without dividing by the \S+-unit step|reads h's", "rise_reported_as_rate"),
 (r"reports the (output|balance|tree height)|substitutes back|instead of the number of", "answers_intermediate_value"),
 (r"forgets to multiply by the", "answers_intermediate_value"),
 (r"reaches \S+ but reports \S+ without dividing", "answers_intermediate_value"),
],
"QR.3.8":[
 (r"a common factor but not the greatest|still share a factor", "gcf_not_greatest"),
 (r"copies both original terms inside without dividing", "gcf_written_terms_not_divided"),
 (r"divides the x-term by the GCF|copies the constant \S+ unchanged inside", "only_one_term_divided_by_gcf"),
 (r"combines the unlike terms", "combines_unlike_terms"),
 (r"forgets to multiply|copies the \S+ unchanged|distributing to only the first term|applies the minus to the x but not|distributes the \S+ correctly to get \S+ but forgets|multiplies \S+ but forgets to apply|distributes the \S+ to the x but copies", "partial_distribution"),
 (r"adds the \S+ and the \S+ (to form|instead of multiplying)|then adds the", "adds_instead_of_scales"),
 (r"mishandles the sign on|mishandles the double negative", "double_negative_mishandled"),
 (r"ignores the minus in front of the second group", "drops_negative_on_group"),
],
# ---------------------------------------------------------------- QR unit 4
"QR.4.1":[
 (r"only the additional|only to the additional|applies the rate only to the|multiplies the unit price by the increase", "rate_applied_to_increment_only"),
 (r"tier rates|reverses the tier", "tier_rates_reversed"),
 (r"inverts the scale factor|original (quantity|count|gallons|weight|time) over the new|divides the (known cost|new cookie count) by|multiplies by the scale factor instead of dividing|scaling the wrong direction|divides by \S+ instead of multiplying", "inverts_conversion_direction"),
 (r"adds it to the original page count|adds a flat|adds the discount instead of subtracting|subtracts a flat|adds the two distances instead of subtracting|converts both heights and adds", "adds_instead_of_subtracts"),
 (r"finds the (unit price|scale factor|rate|price per pound|hourly rate|number of)|reports it as (the final answer|cups|the distance|the answer|cars)|without scaling|without multiplying|computes the (tip amount|discount amount|runner's distance|cyclist's distance)|forgetting to add|instead of subtracting it|reports it instead of the difference|forgets to convert|converts only", "answers_intermediate_value"),
 (r"treating \d+% as|multiplies the bill by \S+ instead of|computes only \d+% of|subtracts \d+% of the new price|applies a \d+% increase again|subtracts another", "percent_applied_forward_not_reversed"),
 (r"cross-multiplies to get \S+ but reports", "answers_intermediate_value"),
 (r"assumes the original height equals the original width|confusing the two dimensions|subtracts the width difference", "reads_wrong_category"),
 (r"uses only one day's|ignoring the \S+-hour interval", "wrong_period_count_used"),
],
"QR.4.2":[
 (r"adds the (fee|booking fee|pickup fee|setup cost|handling fee|registration fee) (to|instead)|charging the fee on every|treating the fee as part of the per-hour", "flat_fee_merged_into_rate"),
 (r"without first subtracting|without subtracting the (fixed|handling|registration|setup)|divides the total by the (per-item price|hourly rate|per-case cost)|divides the budget by the per-case", "divides_before_subtracting_fee"),
 (r"counts only \S+ (months|guests) instead", "wrong_period_count_used"),
 (r"reports only the (flat|signup|booking|fixed|per-guest)|reports only the", "omits_variable_term"),
 (r"computes only the (labor|monthly|ticket|mileage|per-guest) (cost|charges)|forgets (to add )?the (flat|one-time|booking|pickup|fixed)|omits the (per-class|per-box|per-page)", "omits_constant_term"),
 (r"divides the total by the registration fee|divides the setup fee by|adds the two monthly rates", "rates_summed_instead_of_differenced"),
 (r"reverses which is cheaper|naming (Option B|Company Y)|picks Shop (Y|Z)|ignoring that its", "reads_wrong_category"),
 (r"reports that dollar amount as the number|forgetting to divide by|solves the equation correctly to find|reports the number of drinks|reports Company A's full total|counting the setup fee twice|ignores the (drink revenue|weekly transit)|divides the total revenue by the drink price|matches only Company A's hourly rate", "answers_intermediate_value"),
 (r"already saved|dividing the full goal", "omits_second_component"),
],
"QR.4.3":[
 (r"treats the draining as (adding|filling)|using a positive rate|treats the draining tank as also filling|keeps it positive", "sign_wrong_for_decreasing_rate"),
 (r"uses the \S+-(month|minute) (total|volume)|uses the year\S* value|uses the given height \S+ directly|directly as the starting value", "later_total_used_as_starting_value"),
 (r"uses the total (change|volume drop) \S* ?as the (rate|per-minute rate)|without dividing by the", "total_change_used_as_rate"),
 (r"adds the starting amounts", "starting_values_summed_not_differenced"),
 (r"swaps the two (starting volumes|rates)", "model_parameters_swapped_between_entities"),
 (r"swaps the (rate and the starting|flat charge|base fare|starting height|monthly rate|two numbers)|treating \S+ as the (rate|weekly rate)|swaps the rate", "slope_intercept_swap"),
 (r"combines the (flat charge|base fare|starting height|starting amount)|adds the starting amount and the weekly rate|into (a single|one) rate", "flat_fee_merged_into_rate"),
 (r"omits the (flat|base fare|starting|one-time|starting height|starting members)|writes only the rate term", "omits_constant_term"),
 (r"reverses the structure|subtracting the starting volume", "adds_instead_of_subtracts"),
 (r"changes (the fixed revenue|both the rate)|adds \S+ to the existing rate instead of replacing", "model_parameters_swapped_between_entities"),
 (r"divides the total by the per-guest rate without subtracting|adds the base fee to the total|subtracts the per-guest rate once", "divides_before_subtracting_fee"),
 (r"uses the number of additional months", "wrong_period_count_used"),
 (r"selects the plan with the largest starting|largest starting-amount-plus-rate|confusing the initial amount", "intercepts_compared_instead_of_slopes"),
 (r"correctly identifies Plan Z", "slope_intercept_swap"),
 (r"finds the correct rate but omits the starting value entirely", "omits_constant_term"),
 (r"computes the rate magnitude as \S+ but keeps it positive", "sign_wrong_for_decreasing_rate"),
],
})

TOPIC_RULES.update({
# ------------------------------------------------------------------------ AR
"AR.1.1":[
 (r"as multiplication|interpreting f\(\S+\) as f ×", "function_notation_as_multiplication"),
 (r"as a fraction|f divided by", "function_notation_as_fraction"),
 (r"reverses the roles of input and output", "input_output_reversed"),
 (r"axis of symmetry|symmetric partner|opposite sign of|f\(k\) = f", "symmetric_partner_misidentified"),
 (r"sign flip on the constant|drops the negative sign|fails to square the negative", "drops_negative_sign"),
 (r"omits the \S+ step|ignores the \S+ and solves", "omits_constant_term"),
 (r"adds \S+ to both sides instead of subtracting", "adds_instead_of_subtracts"),
 (r"forgets to divide", "answers_intermediate_value"),
 (r"reverses the inequality direction|considers only (positive|negative) x-values", "absolute_equation_one_case_only"),
 (r"subtraction before multiplication|violating order of operations", "order_of_operations_violated"),
],
"AR.1.2":[
 (r"checks only the y-values|scans y-values only|focuses on the repeated output|focusing on the y-values|whether outputs repeat|misreads the repeated output|output \S+ appearing twice is irrelevant|all outputs must be distinct|repeated outputs", "outputs_checked_not_inputs"),
 (r"one-to-one|injective", "injectivity_used_as_function_test"),
 (r"converse|inverts the logic", "function_rule_converse_applied"),
 (r"majority rule|single repeated-input violation", "single_violation_dismissed"),
 (r"vertical lines|vertical line", "vertical_line_test_misapplied"),
 (r"sees 'x = y²'|clean structure|absolute value always simplifies|sideways parabola|overgeneralizes|positive k restricts", "family_matched_by_surface_growth"),
],
"AR.1.3":[
 (r"swaps? (the roles of )?domain and range|assigning the (output|y-interval)|swaps them", "domain_range_swap"),
 (r"endpoint (y-)?values only|two endpoint|endpoint coordinates|discrete sets of the endpoint", "range_as_endpoints_only"),
 (r"multiset|lists the repeated output \S+ twice|repeating \S+ twice|treating the range as an ordered list", "set_listed_as_multiset"),
 (r"shifts the range up|leaves the domain unchanged|subtracts \S+ from the domain|shifting the domain left|horizontal shift", "shift_applied_to_wrong_axis"),
 (r"open boundary|closed at both ends|excluded from the first piece|open at", "piecewise_boundary_openness_error"),
 (r"jump discontinuity|single continuous interval", "jump_discontinuity_ignored"),
 # Split 2026-08-13. These two describe distinct errors, and neither is a swap:
 # domain_range_swap means output values assigned to the domain AND input values
 # to the range. Bundling them made a wrong pick on either option record the
 # same misconception as the genuine swap option alongside it.
 (r"incorrectly adds it to the domain", "output_value_assumed_in_domain"),
 (r"drops input \S+ from the domain", "repeated_output_excludes_input"),
 (r"preserves sign|non-negative|without squaring", "sign_error_on_constant"),
],
"AR.1.4":[
 (r"exponent (in the expression|notation|position)|variable to appear in the exponent|variable base with fixed exponent|reads .{0,20}as '?\S+ to the x|multiplicative coefficient", "exponent_position_confused"),
 (r"second differences|first differences|consecutive (differences|ratios)|constant differences|constant ratios", "differences_vs_ratios_confused"),
 (r"zero second differences", "zero_second_differences_read_as_quadratic"),
 (r"pattern-match|associates|magnitude-based rule|through the origin|upward curvature|sees squared terms|product of two|sees the coefficient|observes that y-values increase|growing and associates|sees '?\S+²'?", "family_matched_by_surface_growth"),
],
"AR.1.5":[
 (r"denominator-cannot-be-zero|defined for all real numbers|'x cannot be zero'|default rule|denominator restriction|division by x", "denominator_zero_rule_not_applied"),
 (r"reads the constant term|treats the constant term|coefficient of the middle term|takes the excluded value to be", "constant_read_as_excluded_value"),
 (r"numerator .{0,30}(zero|equal to zero)|zero numerator|confusing an undefined output|zero output", "zero_numerator_treated_as_undefined"),
 (r"cancels the common factor|cancellation does not restore|simplified form", "cancellation_assumed_to_restore_domain"),
 (r"strict inequality|endpoint x = \S+ is excluded|incorrectly excluding", "radical_endpoint_strictness_error"),
 (r"profitability|contextual", "contextual_condition_as_domain_restriction"),
 (r"sign error|drops the negative sign|misreads the sign of the constant", "sign_error_on_constant"),
 (r"forgets to divide|correctly (finds|isolates) \S+ but forgets", "answers_intermediate_value"),
 (r"adding \S+ to both sides instead of|inverts the division step", "reversed_division"),
 (r"factors incorrectly|reversing the signs", "wrong_sign_on_factor"),
 (r"applies the non-negative rule to x itself|applies only the (radical|denominator) restriction|thinks dividing by a variable|believes squaring the denominator|assumes \S+ has the same restriction", "denominator_zero_rule_not_applied"),
],
"AR.2.1":[
 (r"subtracts the coefficient instead of dividing|adds the coefficient instead of dividing|subtracts the coefficient|subtracts the denominator|adds the denominator", "coefficient_subtracted_not_divided"),
 (r"divides \S+ by \S+ (before|first|without)|skips the constant and divides|divides \S+ by the wrong number|divides by \S+ again|divides by \S+ instead of", "divides_before_isolating"),
 (r"identity|contradiction .{0,30}as meaning|true for all x", "contradiction_confused_with_identity"),
 (r"cancels the \S+ terms and misinterprets", "contradiction_read_as_solution"),
 (r"sign error", "sign_error_on_constant"),
 (r"adds \S+ to (the right side|both sides) instead of subtracting|subtracts \S+ from the right side instead of adding|subtracts the constants instead of adding|adds \S+ to both sides", "adds_instead_of_subtracts"),
 (r"multiplies instead of divides", "multiplies_instead_of_divides"),
 (r"forgets to divide|reads the right-hand side|stops there", "answers_intermediate_value"),
 (r"subtracts \S+ from \S+ rather than|subtracts \S+ from the constant term|drops the \S+ from the right side|subtracts \S+x from the left side only", "subtracts_in_wrong_order"),
 (r"distributes correctly", "sign_error_on_constant"),
],
"AR.2.2":[
 (r"flips the inequality|forgets to flip|fails to flip|without justification|without cause", "inequality_direction_not_flipped"),
 (r"changes the left boundary|changes the right boundary|uses ≥ instead of >|≤ instead of|strict|non-strict|includes x =|excluding x =", "boundary_strictness_changed"),
 (r"drops the negative|ignoring the negative coefficient|without applying the negative sign", "drops_negative_sign"),
 (r"adds \S+ to the right side instead of subtracting|subtracts \S+ from the right side instead of adding|adds the constants instead of subtracting|adds \$?\S+ instead of subtracting|moves \S+ incorrectly", "adds_instead_of_subtracts"),
 (r"subtracts in the wrong order", "subtracts_in_wrong_order"),
 (r"divides \S+ by \S+ instead of multiplying|subtracts \S+ from \S+ instead of multiplying", "divides_instead_of_multiplies"),
 (r"divides \S+ by \S+ without first adding", "divides_before_isolating"),
 (r"forgets to divide|stops before dividing|ignores the \$?\S+ already earned", "answers_intermediate_value"),
],
"AR.2.3":[
 (r"adds the (x-value|rate of change|number of (miles|texts))|treating x as the increment|adds \S+ to the previous output", "input_added_instead_of_rate"),
 (r"adds the flat fee twice|adds \S+ twice", "fixed_fee_added_twice"),
 (r"adds the number of (miles|texts) to the (flat fee|monthly fee)|before multiplying", "input_added_to_fee_before_multiplying"),
 (r"treats the slope as positive|drops the negative sign", "drops_negative_sign"),
 (r"forgets to add the (\$?\S+ )?(flat fee|monthly fee|y-intercept|initial temperature)|computes (only )?the (per-mile|per-text) charge|stops after computing the slope portion|computes the slope times", "omits_constant_term"),
 (r"returns the slope|reports the slope", "omits_variable_term"),
 (r"misidentifies T\(\S+\) as the heating rate|confusing the y-intercept|uses the given y-value|reads the cost from the y-intercept|uses \S+ instead of \S+ as the slope", "slope_intercept_swap"),
 (r"sign error", "sign_error_on_constant"),
 (r"ignores the coefficient and adds x|adds all three numbers", "combines_unlike_terms"),
 (r"substitutes x = \S+ instead of|identifies the rate of change as", "reads_wrong_category"),
 (r"adds \S+ to both sides instead of subtracting|subtracts \S+ instead of adding", "adds_instead_of_subtracts"),
 (r"divides \S+ by \S+ without subtracting|divides by \S+ \(doubling|forgets to divide", "divides_before_isolating"),
 (r"uses half the correct per-text rate", "reads_wrong_category"),
],
"AR.2.4":[
 (r"checks only the first equation|without testing|tests only|substitutes .{0,30}and concludes it is (the unique|it is the solution)|concludes it is the unique solution|treating one valid point", "only_one_equation_checked"),
 (r"adds the constants from both equations|adds correctly to find x|adds \S+x and y terms|assumes x = y|sees x \+ y", "constants_added_to_solve"),
 (r"hybrid expression|adding the slope from one equation|substitutes x = \S+ into the coefficient only|applies the \S+y sign to the whole substitution", "hybrid_expression_substituted"),
 (r"same coefficients = parallel|parallel lines = no solution", "identical_lines_read_as_parallel"),
 (r"reverse|reverses the ordered pair|reverses x and y|writes the pair as", "coordinates_swapped"),
 (r"y-intercept of|finds where y", "reads_wrong_category"),
 (r"sets y = \S+ instead of back-substituting|forgets to divide|arithmetic error during back-substitution", "answers_intermediate_value"),
 (r"divides by \S+ instead of", "divides_before_isolating"),
 (r"Satisfies the total count", "only_one_equation_checked"),
 (r"sign error", "sign_error_on_constant"),
],
"AR.2.5":[
 (r"dashed line as solid|solid but|dashed|solid line|boundary type|uses ≥ instead of >|≤ instead of <|non-strict|strict inequality .{0,30}as|misreads the strict|thinks ≤|boundary points never satisfy|incorrectly including the boundary|accepts the boundary point", "boundary_style_mismatched"),
 (r"shades the wrong side|shading|below instead of above|region above|region below|inverts the direction|flips both the direction|applies the same direction|inverts the first constraint|places the entire region", "wrong_side_shaded"),
 (r"tests only|without checking the (first|second)|after checking only the first pair|does not check the inequality carefully|may not substitute carefully", "only_one_constraint_tested"),
 (r"reverses the comparison|reads \S+ < \S+ as true|confusing the direction of the number line|mistakes this for a non-solution|overlooks this pair", "substitution_comparison_misread"),
 (r"unwarranted domain restriction", "unwarranted_domain_restriction_added"),
 (r"run/rise", "slope_run_over_rise"),
 (r"may miscompute|fail to apply the coefficient", "substitution_comparison_misread"),
],
"AR.2.6":[
 (r"Δy = \S+ but uses it directly|uses Δy = \S+ as the slope|without dividing by Δx", "delta_y_used_as_slope"),
 (r"computes Δx = \S+ and (reports|uses)|uses this as the constant", "delta_x_used_as_slope"),
 (r"y-coordinate of the first point directly as b|given y-coordinate \(\S+\) directly as the y-intercept|uses the given y-coordinate|without solving for the actual y-intercept|without computing: y =|without solving: y =", "point_y_used_as_intercept"),
 (r"horizontal line|vertical line|assigns it to x rather than y", "horizontal_vertical_line_confused"),
 (r"interprets '?slope = \S+'? as the equation", "slope_value_written_as_equation"),
 (r"inverts the formula", "slope_run_over_rise"),
 (r"swaps the (roles of slope|slope and y-intercept)|places \S+ as the coefficient", "slope_intercept_swap"),
 (r"ignores the negative sign|drops the negative sign|negates the slope|applies the negative to the constant|drops the negative", "drops_negative_sign"),
 (r"subtracts instead of divides", "reversed_division"),
 (r"adds slope and y-value", "adds_instead_of_scales"),
 (r"confuses the function's output|confuses the independent variable|flat fee concept but assigns it to the slope", "slope_intercept_swap"),
 (r"copies the original equation|parallel .{0,20}with perpendicular|negative reciprocal", "reciprocal_without_negation"),
 (r"distribution step as|distributes to x but not", "partial_distribution"),
],
"AR.2.7":[
 (r"takes the reciprocal .{0,30}but forgets to negate|reciprocal relationship with the negative-reciprocal|slope and its reciprocal", "reciprocal_without_negation"),
 (r"negates the slope but forgets to take the reciprocal|same magnitude but opposite signs|sign symmetry", "negation_without_reciprocal"),
 (r"raw x-coefficients|compares x-coefficients|coefficient structure|divides each equation by|numbers \S+ and \S+ appear in both|equal x-coefficient magnitude|coefficient of y in Line|differing coefficients", "raw_coefficients_compared"),
 (r"same slope = parallel|without checking the y-intercept|share both|reports the same slope as Line", "parallel_vs_identical_not_distinguished"),
 (r"selects the perpendicular pair", "raw_coefficients_compared"),
 (r"drops the negative sign|sign arithmetic error|treats the \S+ as positive rather than distributing the negative", "drops_negative_sign"),
 (r"returns the original line equation", "parallel_vs_identical_not_distinguished"),
 (r"fails to compute their product", "raw_coefficients_compared"),
],
"AR.2.8":[
 (r"multiplies d by r instead of dividing|misreads the additive formula as a product|divides P by", "wrong_inverse_operation_chosen"),
 (r"inverts the fraction|placing r in the numerator", "numerator_denominator_swap"),
 (r"subtracts r from d instead of dividing|adds them instead of subtracting|divides by c rather than subtracting|subtracts y₁ from both sides instead of adding|subtracts b₂h from both sides instead of dividing|adds y₁ to m before multiplying|adds \(\S+ − b\) to both sides instead of dividing|divides by \(x₂ − x₁\) instead of multiplying|divides by b \(the coefficient", "wrong_inverse_operation_chosen"),
 (r"division distributes over addition|c/\(a \+ b\) as c/a", "division_distributed_over_sum"),
 (r"does not recognize x as a common factor|combines a and b by multiplying|adds it to x instead of subtracting", "common_factor_not_extracted"),
 (r"skips multiplying by", "partial_distribution"),
],
})

TOPIC_RULES.update({
"AR.3.1":[
 (r"without checking the sum|does not check the sum|not summing to", "factor_pair_sum_unchecked"),
 (r"without first extracting the GCF|factors into binomials without", "gcf_not_extracted_first"),
 (r"forgets to write the \S+ outside|only half of the original", "gcf_dropped_after_factoring"),
 (r"mis-factors the trinomial", "factor_pair_sum_unchecked"),
 (r"flips the sign on one factor|wrong signs on both factors|both factors negative|signs to the wrong factor|keeps the magnitude \S+ but the wrong sign|flips the sign on the|difference of squares and mirrors the sign", "wrong_sign_on_factor"),
 (r"ignores the negative sign on the constant|ignores the negative on the constant|takes the product of \S+ and \S+ as \S+, ignoring the negative|treats the product as", "drops_negative_sign"),
 (r"adds the magnitudes|reverses the sign when combining", "sign_error_on_constant"),
 (r"treats the constant \S+ itself as a factor magnitude|subtracts the known constant", "factor_pair_sum_unchecked"),
],
"AR.3.2":[
 (r"perfect square trinomial|difference of squares|squares the second term|recognizes a perfect square but uses the wrong sign|does not recognize the perfect square", "perfect_square_vs_difference_of_squares"),
 (r"ignores the leading coefficient|as if the quadratic were monic|splits the leading coefficient", "leading_coefficient_ignored_in_factoring"),
 (r"swaps which binomial|assigns the constants to the wrong binomials|right four numbers", "constants_assigned_to_wrong_binomials"),
 (r"without checking the sum|does not require the middle term to be zero|mis-splits the factors", "factor_pair_sum_unchecked"),
 (r"forgets to write the \S+ outside|only one third of the original", "gcf_dropped_after_factoring"),
 (r"leaves the trinomial unfactored|not completely factored", "stops_before_simplifying"),
 (r"flips the sign on one factor|wrong signs|both factors negative|negative sign on the wrong factor|puts the negative sign|sign error", "wrong_sign_on_factor"),
 (r"ignores the negative constant|treats the product as", "drops_negative_sign"),
],
"AR.3.3":[
 (r"without flipping (the )?signs?|reports the numbers inside the factors|takes the sign of the constant term in each factor|ignores the signs entirely|reads the numbers inside", "factor_constants_read_as_roots"),
 (r"ignores the leading coefficient", "leading_coefficient_ignored_in_root"),
 (r"does not rearrange into standard form|reads the constant and the \S+-coefficient from the original|constant term of the original equation", "not_rearranged_to_standard_form"),
 (r"unfactorable sum|no real solutions", "perfect_square_confused_with_unfactorable_sum"),
 (r"difference of squares", "perfect_square_vs_difference_of_squares"),
 (r"wrong factor pair|picks another wrong factor pair|ignores the negative on the constant|assigns both signs as negative", "factor_pair_sum_unchecked"),
 (r"flips the sign on only one factor|flipped sign|flips the sign on the second solution|drops the negative sign|sign error", "wrong_sign_on_factor"),
 (r"finds the other solution", "reads_wrong_category"),
],
"AR.3.4":[
 (r"uses \S+ instead of \S+ in the numerator", "quadratic_formula_wrong_numerator_sign"),
 (r"forgets to divide by|computes \S+ directly, giving|forgets to divide the whole numerator", "quadratic_formula_denominator_omitted"),
 (r"divides only the radical term|divides by \S+ instead of \S+:", "quadratic_formula_partial_division"),
 (r"discriminant as \S+ instead of", "discriminant_miscomputed"),
 (r"negative discriminant with a single repeated", "negative_discriminant_read_as_one_root"),
 (r"axis of symmetry", "axis_of_symmetry_reported_as_root"),
 (r"does not use the formula|reads the coefficient|does not rearrange into standard form|reads the coefficients on the right side", "not_rearranged_to_standard_form"),
 (r"simplifies the radical but forgets to divide", "quadratic_formula_denominator_omitted"),
],
"AR.3.5":[
 (r"taking the \S+ inside the parentheses literally|misreads the sign of \S+, taking", "vertex_form_h_read_literally"),
 (r"reports the \S+-coordinate of the vertex|finds the axis of symmetry \S+ and reports it as the minimum|computes the vertex \S+-coordinate instead of the axis|finds the time at which the maximum occurs|confusing the input with the output|reporting a \S+-value when asked", "axis_reported_as_extreme_value"),
 (r"reverses the direction rule|associating a (negative|positive) leading coefficient|thinks the constant \S+ determines whether", "opening_direction_rule_reversed"),
 (r"reports the constant term|the initial height at|reports the coefficient \S+ from the equation", "constant_term_reported_as_extreme"),
 (r"no extreme point|has no maximum or minimum", "extreme_point_believed_absent"),
 (r"swaps the (two )?coordinates", "coordinates_swapped"),
 (r"flips the sign of|drops the sign|reversing the direction and misplacing|drops the leading negative|forgets the leading negative", "sign_error_on_constant"),
 (r"adds the two vertex coordinates", "combines_unlike_terms"),
 (r"divides by \S+ instead of \S+, or omits the division|forgets to multiply by", "quadratic_formula_denominator_omitted"),
 (r"sets \S+ equal to the target axis value directly", "axis_reported_as_extreme_value"),
],
"AR.3.6":[
 (r"same signs as the intercepts|flips the sign(s)? (inside|on)|writes the factors with", "factor_signs_match_intercepts"),
 (r"stops at \S+ without using the given point|does not pass through|uses \S+ instead of solving with the point|wrong leading coefficient", "given_point_not_used"),
 (r"reverses the (horizontal|vertical|both) shift", "transformation_shift_direction_reversed"),
 (r"single touch point as two separate intercepts|squares a single factor|sum of squares", "double_root_written_as_two_intercepts"),
 (r"wrong sign on the leading coefficient|forgets the negative leading coefficient|opens (downward|upward)|drops the negative coefficient|wrong sign on \S+:", "wrong_sign_on_factor"),
 (r"misreads the sign of \S+, writing|flips the sign of \S+, writing", "vertex_form_h_read_literally"),
 (r"wrong value for the second intercept|crosses at", "factor_signs_match_intercepts"),
],
"AR.3.7":[
 (r"forgets to subtract it back|leaving the original constant|skips the balance adjustment|carries the original constant", "balance_term_not_subtracted_back"),
 (r"forgets to multiply the \S+ balance term|by the factored-out", "balance_term_not_scaled_by_leading_coefficient"),
 (r"takes half of \S+ as|subtracts \S+ from the constant in the wrong direction", "half_of_b_miscomputed"),
 (r"squares only the first and last terms|dropping the middle term", "binomial_square_middle_term_omitted"),
 (r"wrong sign on the middle term", "sign_error_on_constant"),
 (r"reverses the (horizontal|vertical) shift|omits the reflection|overlooks the negative leading coefficient", "transformation_shift_direction_reversed"),
 (r"misreads the sign of|flips the sign of|takes the \S+ inside the parentheses literally", "vertex_form_h_read_literally"),
 (r"swaps the coordinates", "coordinates_swapped"),
 (r"axis of symmetry as", "axis_reported_as_extreme_value"),
 (r"distributes the \S+ to the \S+ and \S+ terms but not", "partial_distribution"),
],
"AR.4.1":[
 (r"adds the exponents of the like terms", "exponents_added_when_combining"),
 (r"keeps the sign of the first term", "sign_of_first_term_kept"),
 (r"multiplies the like-term coefficients", "multiplies_coefficients"),
 (r"treats (every|all|the constants?|the \S+ term|\S+ and \S+ as) .{0,40}like terms?|adds all (four|the) coefficients|adds every coefficient|collapsing", "combines_unlike_terms"),
 (r"drops the (final|third|lone|second term)|combines only", "drops_unlike_term"),
 (r"treats \S+ as \S+, (computing|getting)|treats \S+ as \S+:|subtracts the coefficient \S+ from the constant|sign error|applies the negative only to the first term|fails to apply the negative|adds the second group instead of subtracting|distributes \S+ instead of \S+ to the second group", "drops_negative_on_group"),
 (r"multiplies each coefficient only by the first term|leaving the second terms unmultiplied|forgets to multiply", "partial_distribution"),
],
"AR.4.2":[
 (r"First and Last terms only|omitting the Outer and Inner|omits the Inner product|includes the Outer product", "foil_outer_inner_omitted"),
 (r"cross product only once|squares each term independently and omits the middle term", "cross_product_counted_once"),
 (r"keeps the larger of the two exponents", "larger_exponent_kept"),
 (r"multiplies the exponents instead of adding", "multiplies_exponents_wrongly"),
 (r"adds the coefficients instead of multiplying", "adds_instead_of_scales"),
 (r"treats \S+ as \S+ \(failing to add exponents\)", "adds_exponents_wrongly"),
 (r"adds the coefficient and the constant|adds the last terms instead of multiplying", "adds_instead_of_scales"),
 (r"wrong sign|treats the subtraction as addition|drops the negative sign|makes both middle products negative|treating \S+ as negative|makes a sign error|combines the \S+ terms with the wrong sign|adding the magnitudes", "sign_error_on_constant"),
 (r"distributes \S+ to the first term only|multiplies only the variable|distributes only the first term|never multiplies by", "partial_distribution"),
],
"AR.4.3":[
 (r"combines all variable terms regardless of degree|summing \S+ into a single|merges the \S+ and \S+ terms as if they were like", "terms_of_different_degree_combined"),
 (r"carries only the first \S+ term and forgets|combines like terms but drops the|carrying only the \S+ from the second polynomial", "polynomial_term_never_carried"),
 (r"overlooks the implied coefficient", "ignores_unit_coefficient"),
 (r"ignores (the negative|both negative)|treats the \S+ as \S+, adding magnitudes|assigns it a positive sign|adds the magnitudes of the x-coefficients", "drops_negative_sign"),
 (r"distributes the negative to the first term only|leaves the constant \S+ unchanged|leaves the middle \S+ unchanged|distributes the subtraction to the first term|multiplies only the first term", "drops_negative_on_group"),
 (r"adds instead of subtracting|adds all three polynomials|treats the subtraction as addition", "adds_instead_of_subtracts"),
 (r"distributes \S+ but computes", "sign_error_on_constant"),
],
"AR.4.4":[
 (r"multiplies the exponents", "multiplies_exponents_wrongly"),
 (r"subtracts (the exponents|in the wrong order|correctly to get an exponent)|reverses the subtraction order", "subtracts_exponents_wrongly"),
 (r"adds the exponents|adds only the written exponents", "adds_exponents_wrongly"),
 (r"coefficient of \S+ because there are (two|three) factors|attaches a coefficient", "combines_unlike_terms"),
 (r"raises the inner exponent to the outer|keeps only the outer exponent", "inner_exponent_raised_not_multiplied"),
 (r"negative exponent as a negative sign|interprets the negative exponent", "negative_exponent_as_negative_value"),
 (r"drops the negative sign on the exponent|fails to make the exponent positive|drops the negative sign on the denominator", "drops_negative_sign"),
 (r"ignores the implied exponent", "ignores_unit_coefficient"),
 (r"multiplies the coefficient by the outer exponent|raises only the variable|fails to cube the coefficient|forgets to divide the coefficient", "coefficient_not_raised_to_power"),
 (r"overlooks the second factor", "polynomial_term_never_carried"),
],
"AR.4.5":[
 (r"cancels the \S+ terms directly|invalid term-level cancellation|cancels the lone \S+|cancels a single term instead of factoring|treats \S+ as canceling away|cancels \S+ correctly but distributes", "term_level_cancellation"),
 (r"over-cancels|removing the single \S+ .{0,30}against both", "over_cancellation"),
 (r"factors (the numerator|the denominator).{0,60}(sign error|incorrectly)|treating the (difference of squares|perfect-square trinomial)|forgets the common factor", "wrong_sign_on_factor"),
 (r"reads the restriction only from the simplified", "restriction_read_from_simplified_form"),
 (r"sets the numerator|combines the zeros of the numerator|over-including the numerator zero", "numerator_zeros_confused_with_undefined"),
 (r"divides only the (first|second) term|copies the \S+ term undivided|divides the constant terms|leaves \S+ undivided|reduces the coefficient", "partial_distribution"),
],
"AR.4.6":[
 (r"adds the denominators|subtracts the denominators|adds the denominators along with|treats the denominators as unrelated", "denominators_combined_with_numerators"),
 (r"without rescaling|without adjusting|rescales the first fraction but leaves the second|rescales each numerator by its own denominator|unadjusted", "numerators_not_rescaled"),
 (r"distributes the negative sign to only the first term|applies the negative sign to only the first term", "negative_not_distributed_across_numerator"),
 (r"identical denominators as different and multiplies", "identical_denominators_multiplied"),
 (r"multiplies the numerators instead of adding|adds the numerators instead of subtracting|adds instead of subtracting|subtracts the numerators in the reverse order|multiplies the identical denominators", "adds_instead_of_subtracts"),
 (r"distributes \S+ incorrectly", "partial_distribution"),
],
"AR.4.7":[
 (r"substitutes only into the numerator|ignores the denominator", "numerator_only_substituted"),
 (r"reads the (denominator|numerator)|ignores the \S+ in the denominator|drops the coefficient \S+ in the (numerator|denominator)|treats \S+ as \S+|computes \S+ as \S+, giving a denominator|evaluates only the \S+ term|drops the \S+ in the denominator|drops the negative when computing", "denominator_misread"),
 (r"sets (only )?the numerator equal", "numerator_set_equal_to_output"),
 (r"multiplies the numerator by the denominator|adds the numerator and the denominator", "reversed_division"),
 (r"subtracts in the reverse order", "subtracts_in_wrong_order"),
 (r"factors with the wrong signs|factor pair", "wrong_sign_on_factor"),
 (r"sets the numerator equal to zero instead of the denominator", "numerator_zeros_confused_with_undefined"),
 (r"confuses input with output", "input_output_reversed"),
 (r"multiplies only the first term inside the parentheses", "partial_distribution"),
],
"AR.4.8":[
 (r"not the largest perfect-square factor|does not simplify the result|still has the perfect-square factor|does not simplify the variable part|does not reduce", "largest_perfect_square_not_extracted"),
 (r"moves \S+ outside the radical instead of its square root|pulls the \S+ outside the radical, leaving the perfect square|treats \S+ as a perfect square, dropping|removes the perfect-square factor \S+ without carrying", "perfect_square_moved_out_unrooted"),
 (r"adds the radicands as well as the coefficients|subtracts the radicands as well|adds the coefficients and the radicands", "coefficients_and_radicands_both_combined"),
 (r"multiplies only the (denominator|numerator)|rationalizes correctly to \S+ but does not reduce", "rationalisation_applied_to_one_part"),
 (r"splits the radical over addition|does not distribute|adds the radicands instead of multiplying|subtracts the values under one radical|adds the values inside the radical", "false_radical_distribution"),
 (r"evaluates \S+ as \S+ treating the square root as half", "halves_the_radicand"),
 (r"wrong positions", "numerator_denominator_swap"),
 (r"adds the coefficients instead of subtracting", "adds_instead_of_subtracts"),
 (r"divides the radicand by \S+ instead of", "divides_instead_of_multiplies"),
],
"AR.4.9":[
 (r"squares before isolating|treating each piece separately|does not square the right side|isolates correctly to \S+ but does not square", "squares_before_isolating"),
 (r"doubles the \S+ instead of squaring|doubles instead of squaring", "squaring_confused_with_doubling"),
 (r"does not check the candidates|accepting the extraneous root|picks the extraneous candidate", "extraneous_root_not_checked"),
 (r"assumes the smaller (root|candidate) must be extraneous|discards \S+ without checking", "valid_root_discarded_as_extraneous"),
 (r"squares the binomial term by term|omitting the middle term", "binomial_square_middle_term_omitted"),
 (r"factors with the wrong signs|reads the numbers inside the factors|reads the coefficient", "wrong_sign_on_factor"),
 (r"adds \S+ (to the right side )?instead of subtracting|subtracts \S+ instead of adding|sign error moving the constant", "adds_instead_of_subtracts"),
 (r"forgets to divide", "answers_intermediate_value"),
],
"AR.4.10":[
 (r"adds the exponents", "adds_exponents_wrongly"),
 (r"divides the exponents|subtracts the exponents|reverses the order of subtraction", "subtracts_exponents_wrongly"),
 (r"multiplies the exponents", "multiplies_exponents_wrongly"),
 (r"cancels the identical bases|multiplies the bases|changing \S+ to|over-cancels|treating \S+ as fully canceling", "bases_multiplied_with_exponents"),
 (r"zero exponent|believes a zero exponent", "zero_exponent_as_zero"),
 (r"negative exponent as negating|interprets \S+ as \S+ rather than|drops the negative sign on the exponent|treating \S+ as", "negative_exponent_as_negative_value"),
 (r"multiplies the base by the exponent|divides the value by the base once|treats \S+ as the product", "base_times_exponent"),
 (r"ignores the exponent and reports the base", "zero_exponent_as_zero"),
 (r"evaluates \S+ as \S+, giving|forgets the \S+ and reports|ignores the lone \S+ in the denominator", "answers_intermediate_value"),
],
"AR.4.11":[
 (r"ignores the coefficient|drops the coefficient", "coefficient_ignored_in_power"),
 (r"applies the exponent to the coefficient|raises the product of coefficient and base", "exponent_applied_to_coefficient"),
 (r"adds the coefficient to the power|adds the coefficient instead of multiplying", "coefficient_added_not_multiplied"),
 (r"multiplying the base by the exponent|interprets \S+ as \S+, multiplying", "base_times_exponent"),
 (r"zero exponent|zero-power rule", "zero_exponent_as_zero"),
 (r"negative exponent as a negation|ignores the negative sign on the exponent|sign error, treating", "negative_exponent_as_negative_value"),
 (r"treats the decay as linear|subtracting \S+% of the original", "linear_instead_of_compound"),
 (r"treats \S+ as the linear term|reports the value of the power|counts the number of multiplication steps|raises the base to the second power instead of the third|applies the factor only once", "answers_intermediate_value"),
],
"AR.4.12":[
 (r"subtracts the rate instead of adding|treats the loss as a gain|treats the scenario as growth|applies a growth model to a decay|applies a decay model to a growth|applies a decay factor|treating the increase as a decrease|applies a growth", "growth_decay_factor_direction_wrong"),
 (r"uses the rate itself as the factor|uses the decimal rate \S+ as the factor|forgetting (that the value retained|to add it)|fails to convert the percent to a decimal", "rate_used_as_factor"),
 (r"applies the rate linearly|simple interest|linear depreciation|uses linear (growth|depreciation)|uses simple growth", "linear_instead_of_compound"),
 (r"compounds annually instead of quarterly|fails to divide the rate by|fails to multiply the exponent", "compounding_period_not_adjusted"),
 (r"reports the (entire growth factor|decay factor)|reads the decimal \S+ directly as a percent|mis-converts the decimal to a percent|reading it as \S+% instead", "factor_reported_as_rate"),
 (r"for only one year|only one year instead|adding \S+% of the original twice", "growth_applied_for_wrong_duration"),
],
})

TOPIC_RULES.update({
# ------------------------------------------------------------------------ GR
"GR.1.1":[
 (r"wrong measurement category|confuses (volume|temperature|distance|area|length|weight or mass) units", "wrong_measurement_category"),
 (r"too (large|small) in magnitude|far too (large|small)|orders of magnitude too large|fails to account for scale|without considering scale|impractical", "unit_magnitude_mismatched"),
 (r"divides by \S+ instead of \S+|moves the decimal point in the wrong direction|conversion factor as \S+ mL", "metric_prefix_factor_wrong"),
 (r"metric unit that is both the wrong system|wrong system for US|substitutes familiar everyday units|Kelvin|customary length unit", "measurement_system_mismatched"),
 (r"linear conversion factor \(×\S+\) to the area|area conversion factor|square yards as if it were|divides the area by", "area_conversion_factor_not_squared"),
 (r"inverts the conversion ratio|direction of the conversion|assumes a \S+-to\S+ relationship|incorrect conversion factor|misremembering the ratio", "inverts_conversion_direction"),
],
"GR.1.2":[
 (r"complementary .{0,20}with supplementary|supplementary .{0,20}with complementary|swapping the two terms|subtracts from \S+° instead of \S+°|applies the (supplementary|complementary) rule (rather|instead)|applies the ratio to a sum of|confusing supplementary with complementary|applying the complementary rule to a vertical", "complementary_supplementary_swap"),
 (r"vertical angles with (complementary|supplementary) angles|confuses vertical angles with supplementary|finds the supplement of|confusing the supplementary relationship of adjacent angles", "vertical_angles_treated_as_supplementary"),
 (r"vertical angle equality|doubles the given angle|'?vertical'? implies a multiplicative|applies a vertical angle relationship|not defined by summing|are equal in measure", "vertical_angles_given_a_sum_rule"),
 (r"selects adjacent|confuses vertical angles with adjacent angles|positional relationship", "adjacent_vs_vertical_confused"),
 (r"sums all four angles|adds the two vertical angles", "all_intersection_angles_summed"),
 (r"skips the first step|completes only the first step|reports only one of the two|reports that value as the answer instead of continuing|uses k itself", "answers_intermediate_value"),
 (r"adds the given angle to \S+° rather than subtracting", "adds_instead_of_subtracts"),
 (r"subtraction error|failing to borrow", "off_by_one_count"),
 (r"misreads the ratio", "reads_wrong_category"),
],
"GR.1.3":[
 (r"one tick mark (to the left|to the right|below|above)|adjacent label|reports the label immediately", "reads_adjacent_value"),
 (r"snaps to the nearest labeled tick|jumps directly to the next labeled|reads only the labeled centimeter", "snaps_to_labelled_tick"),
 (r"unlabeled marks|smallest division|interval size|two-interval increment|extra factor|second unlabeled|bisection", "subdivision_count_miscomputed"),
 (r"reads only the leading digit|dropping the units place", "leading_digit_read_only"),
 (r"maximum label|confusing the scale's endpoint", "reads_adjacent_value"),
 (r"misidentifies which interval|estimates the mercury as halfway|three-quarters", "snaps_to_labelled_tick"),
 (r"omits the \S+ step in the conversion|inverts the conversion coefficient", "temperature_offset_omitted"),
 (r"reports that increment as the answer", "answers_intermediate_value"),
],
"GR.2.1":[
 (r"multiplies by \S+ instead of \S+|number of sides of a rectangle|confusing a (pentagon|hexagon)|treats the triangle as a four-sided|counting the base twice|multiplies by \S+ instead", "wrong_side_count_for_polygon"),
 (r"omits one of the|adds only (two|three|one)|summing only|reports (only )?(the length of )?one side|omits the constant side|omits one of the three", "side_omitted_from_perimeter"),
 (r"treats the figure as a rectangle|approximates the figure as a rectangle|pairing the two smallest|using the two largest|phantom side", "irregular_treated_as_rectangle"),
 (r"notch", "notch_sides_not_accounted"),
 (r"subtracts the number of sides|multiplies the perimeter by the number of sides|divides by \S+ instead of \S+|divides the perimeter by the length", "side_count_subtracted_from_perimeter"),
 (r"multiplies (length by width|all three sides|the two equal sides)|computes an area-like product", "perimeter_area_confusion"),
 (r"subtracts only the length from the perimeter|divides the perimeter by \S+ and reports|reports that sum as the answer|but stops|stops there|without converting|reports only l \+ w|rather than multiplying by", "answers_intermediate_value"),
 (r"uses the length in yards without converting|adds \S+ to the length instead of multiplying", "inverts_conversion_direction"),
 (r"adds the four known sides to the perimeter", "adds_instead_of_subtracts"),
 (r"largest of the four known sides", "reads_wrong_category"),
 (r"evaluates \S+ as \S+x only|uses x = \S+ instead", "partial_distribution"),
],
"GR.2.2":[
 (r"uses the radius .{0,30}instead of the diameter|treats the diameter .{0,20}as the radius|uses it as the radius|treats d as if it should be doubled|uses the radius \(", "radius_diameter_substituted"),
 (r"omits the factor of \S+|using C = πr instead|only half the correct", "factor_of_two_omitted"),
 (r"doubles the (diameter|correct circumference|result a second time)|doubles the circumference coefficient", "result_doubled_twice"),
 (r"does not apply the \S+° fraction|takes half the circumference|instead of a quarter", "arc_fraction_not_applied"),
 (r"area formula|squares the (diameter|radius)|area of a circle|πr²", "circumference_area_confusion"),
 (r"finds the radius instead of the diameter|divides by π only|divides by \S+ only|divides by \S+π twice|multiplies by π ≈|treats \S+π as requiring", "reversed_division"),
 (r"forgets to multiply by the \S+ rotations|but stops there|reports the (original|diameter)|without multiplying", "answers_intermediate_value"),
 (r"converts km to m using a factor|divides by \S+ before converting", "metric_prefix_factor_wrong"),
 (r"compares areas instead of circumferences|difference ratio|multiplies the correct ratio", "area_ratio_confused_with_linear_ratio"),
],
"GR.2.3":[
 (r"adds (length and width|base and height)|adds base and height", "base_height_added_not_multiplied"),
 (r"slant side", "slant_height_used_for_perpendicular"),
 (r"applies the (triangle|circumference|parallelogram|circle) formula|uses πr without squaring|uses the diameter .{0,20}in place of the radius|misapplies the (circle|triangle) formula|formula for the area of a circle", "formula_borrowed_from_other_shape"),
 (r"adds the circle area to the rectangle|does not subtract the circular cutout|ignoring the window", "cutout_added_not_subtracted"),
 # Split 2026-08-13: applying the factor twice is the opposite of omitting it.
 # Must precede the omits_ rule -- first match wins.
 (r"applies the ½ factor twice", "applies_fractional_factor_twice"),
 (r"omits the ½ factor|omits the ½|halves the height", "omits_fractional_factor"),
 (r"computes the perimeter instead of the area|computes the area instead of the perimeter", "perimeter_area_confusion"),
 (r"doubles the (correct )?area|doubles the result|doubles the width", "result_doubled_twice"),
 (r"subtracts the length from the area|multiplies area by length|divides by \S+ instead of \S+|divides the area by the sum of the bases|multiplies the area by \S+ instead of solving|divides by \S+π instead of π", "wrong_inverse_operation_chosen"),
 (r"single rectangle using the combined height|omits the (triangle|rectangle) entirely", "omits_second_component"),
 (r"reports that value as the radius|reports the coefficient \S+ as the answer|but then doubles it|confusing radius with diameter", "forgets_square_root"),
 (r"drops the \S+|evaluates b₂ as x rather than", "partial_distribution"),
],
"GR.2.4":[
 (r"sum of the two bases|reports that (sum|value) as|conflating the sum of dimensions", "sum_of_bases_reported_as_one_base"),
 (r"without first multiplying by|without accounting for the ½|by the height only|by twice the height|divides the area by \S+ only|divides by \S+ \(applying the triangle|omits the ½ factor|without isolating", "one_of_two_divisors_omitted"),
 (r"squares the area", "area_squared_instead_of_rooted"),
 (r"stops there without taking the square root|then halves it rather than taking the square root|reports r² as the radius", "forgets_square_root"),
 (r"subtracts (the width|the length|the base|the known base|\S+ from the area)|multiplies the (area|perimeter) by|divides by \S+ instead of by \S+|divides the perimeter by \S+ and reports|divides by \S+ again|divides the area by \S+ \(getting|divides again by|divides \S+ by \S+ instead of|subtracts the number of sides", "wrong_inverse_operation_chosen"),
 (r"but stops|without subtracting|reports the length instead|reports it as the area|reports that as the area|forgets to add|rounds \S+ to \S+ due to a division error|reports this directly as the missing base|reports it directly as the height|reports that as x", "answers_intermediate_value"),
 (r"reports π ≈", "forgets_square_root"),
 (r"miscounts the x-coefficient|ignores subtracting the constant", "partial_distribution"),
],
"GR.2.5":[
 (r"omits the factor of \S+|without accounting for each face's opposite pair|omits the factor of|missing the three opposite faces|writes the sum of one face in each pair", "opposite_faces_not_doubled"),
 (r"omits (both|the) (circular|triangular|square) base|omits the (square base|circular base)|includes only one (circular|triangular) base", "base_omitted_from_solid"),
 (r"only the lateral|lateral area of the four triangular faces|omits the lateral surface|only the (area of both circular bases|circular base area)", "lateral_surface_only"),
 (r"counts two circular bases instead of one", "both_bases_counted_where_one"),
 (r"shared interface|removes the footprint|without removing", "shared_interface_not_removed"),
 (r"computes the volume|volume formula|cubing the side length|s³|doubles the volume formula", "volume_surface_area_confusion"),
 (r"omits the ½ factor|drops the ½ factor|½ factor", "omits_fractional_factor"),
 (r"replaces lh with l²|uses SA = \S+s instead|uses πr² \(factor|uses \S+πr² \(factor|uses \S+πr instead of \S+πr²|does not square the radius|computes only one face", "formula_borrowed_from_other_shape"),
 (r"without first subtracting|divides by \S+ instead of|without first subtracting the base|reports r² as the radius|takes the square root twice|then doubles it", "forgets_square_root"),
 (r"includes the floor|paints only the floor|product of all three dimensions", "omits_second_component"),
 (r"reverses the comparison|halves the difference|equal volumes imply equal surface areas", "area_ratio_confused_with_linear_ratio"),
 (r"doubles the isolated term", "result_doubled_twice"),
],
"GR.2.6":[
 (r"uses r instead of r²|uses s instead of s²|divides volume by πr \(uses r without squaring|does not square", "radius_not_squared"),
 (r"adds (all three dimensions|the three dimensions)|multiplies only length and width|sum of the three distinct face areas", "dimensions_added_not_multiplied"),
 (r"assumes the cone and cylinder have equal volumes|applies the ⅓ factor twice|uses ½ instead of ⅓|applies the cone formula", "cone_cylinder_ratio_wrong"),
 (r"half the height", "half_height_used"),
 # Split 2026-08-13: dividing by the wrong number is not omitting the factor.
 (r"divides by \d+ instead of \d+", "wrong_fractional_divisor_used"),
 (r"omits the (⅓|½|\(\S+\)) factor|omits the ⅓|divides by \S+ instead of \S+|uses \S+πr³ instead", "omits_fractional_factor"),
 (r"surface area formula|no standard geometric meaning|area of one face", "volume_surface_area_confusion"),
 (r"doubles the (correct )?volume|divides the correct volume by", "result_doubled_twice"),
 (r"cube root|reports r³ as the radius|divides by \S+ instead of taking the cube root|doubles it, confusing radius with diameter", "forgets_square_root"),
 (r"divides volume by π only|divides by \S+πr² \(doubles the denominator", "radius_not_squared"),
 (r"linear instead of quadratic|treating the equation as linear|then halves|correctly solves w²", "area_squared_instead_of_rooted"),
 (r"entire composite height|omits the cone entirely|reports that alone|only the cylinder volume|adds the two volumes instead of subtracting|reports the (cone's|cylinder's) volume as the difference", "omits_second_component"),
 (r"uses x = \S+ as the height|forgets to multiply by the height", "partial_distribution"),
],
"GR.2.7":[
 (r"does not distribute to the|leaves the constant unchanged|distributes \S+ to the x-term only|applies the factor of \S+ to the variable term only|multiplies \S+ by x but does not distribute|distributes \S+ to the x-terms but", "factor_not_distributed_to_constant"),
 (r"phantom constant|no basis in the given dimensions|different value for one side|adds a phantom", "phantom_term_introduced"),
 (r"applies ½ to base only|½ × \S+x = x \(applies ½ to base|ignoring the height", "fractional_factor_applied_to_base_only"),
 # Split 2026-08-13: "correctly applies ⅓ ... but fails to simplify" is the
 # factor reaching only one term of the sum, not the factor being omitted.
 (r"correctly applies ⅓ to the \S+ term but fails to simplify", "fractional_factor_applied_to_one_term_only"),
 (r"omits the (½|⅓) factor|applies a ½ factor|correctly applies ⅓|omits the ⅓", "omits_fractional_factor"),
 (r"expands \(x \+ \S+\)² incorrectly|omitting the middle term|squares \(x \+ \S+\) incorrectly", "binomial_square_middle_term_omitted"),
 (r"computes the (area|perimeter) instead of the (perimeter|area)|multiplies (length by width|l × w)|adds (length and width|l \+ w|width and length|height and base)|squares x in the product", "perimeter_area_confusion"),
 (r"circumference formula|omits the squaring step|uses V = πrh|πr instead of πr²", "circumference_area_confusion"),
 (r"multiplies by \S+ instead of \S+|reports one side length|counts only one x-variable|undercounts the x-coefficient", "wrong_side_count_for_polygon"),
 (r"sign error to the constants|adds only the positive constant|ignores the \S+ in|adds the constants instead of subtracting|reverses the constant subtraction|adds both area expressions", "sign_error_on_constant"),
 (r"omits the factor of \S+|applies the factor of \S+ to", "opposite_faces_not_doubled"),
 (r"doubles the entire expression", "result_doubled_twice"),
 (r"treats \(x \+ \S+\) as if it were|drops the \S+x term|adds x \+ \(x \+", "partial_distribution"),
],
"GR.3.1":[
 (r"adds the two given lengths|subtracts the given lengths|adds the two legs|adds the length and width|adds the three dimensions|adds all the distances|adds the three dimensions directly|sum of the legs|treating the legs as summing|leg sum equal|two legs together equal|subtracts the difference from the hypotenuse", "legs_combined_without_squaring"),
 (r"longest given number must already be the hypotenuse|treats the longer leg as the hypotenuse", "longest_side_assumed_hypotenuse"),
 (r"base diagonal .{0,20}but stops|not continuing to the space diagonal", "space_diagonal_stopped_at_base"),
 (r"resembles the \S+ triple|assumes this resembles", "pythagorean_triple_assumed"),
 (r"leaves (it|the hypotenuse) out of the perimeter|leaves out the originally given leg|adding only the two|leaves the hypotenuse out", "side_omitted_from_perimeter"),
 (r"forgets to take the (final )?square root|but forgets to take", "forgets_square_root"),
 (r"multiplies the two legs|computes the area instead|multiplies length by diagonal|computes the perimeter instead of the area|computes the perimeter instead", "perimeter_area_confusion"),
 (r"reports only the hypotenuse|reports it as the longer leg|uses only the north displacement|uses the (hypotenuse|diagonal) as the second|finds the width|forgetting to take the square root for the width", "answers_intermediate_value"),
],
"GR.3.2":[
 (r"\S*45\S*|hypotenuse rule|√2|factor \S+ instead of the \S+ factor|applies the \S+ factor|uses the \S+ (long-leg )?factor|divides the (hypotenuse|diagonal|leg) by \S+ \(the|applying a \S+ relationship|applies the \S+ hypotenuse rule", "special_triangle_factor_swapped"),
 (r"short leg|long leg|treats the given long leg as if it were the short|treats the hypotenuse \S+ as the short leg|treats both legs as", "short_long_leg_roles_confused"),
 (r"multiplies (the hypotenuse|the short leg|the diagonal|by \S+ instead of )|divides the leg by \S+ instead of multiplying|divides by \S+ rather than|halves the long leg|doubles it", "special_ratio_multiplied_not_divided"),
 (r"finds the hypotenuse .{0,30}instead of the long leg|reports the hypotenuse instead|finds the correct leg of \S+ but reports the hypotenuse|uses the hypotenuse of \S+ as one of the legs", "hypotenuse_reported_for_leg"),
 (r"assumes the (leg|side) equals the (hypotenuse|diagonal)|applying no ratio at all|applying no relationship", "no_ratio_applied"),
 (r"finds the long leg .{0,30}instead of the hypotenuse|correctly finds the short leg of \S+ but stops|stops after halving|reports the short leg", "answers_intermediate_value"),
 (r"forgets to take the square root|forgets the factor \S+ in the area formula|omits the \S+ factor", "omits_fractional_factor"),
 (r"includes only one leg|drops the \S+ on the longer side|area of one of the two right triangles|uses the diagonal of \S+ as the longer side|uses \S+ instead of \S+, solving", "side_omitted_from_perimeter"),
],
"GR.3.3":[
 (r"which is \S+, not|used \S+ instead of (sine|cosine|tangent)|used (cosine|tangent|sine) instead of|reported the (tangent|sine) ratio|used the sine value|applied the \S+ ratio|reported the sine value as the tangent", "wrong_trig_ratio_selected"),
 (r"inverted the (ratio|tangent|cosine)|placed it over the opposite|adjacent over opposite|computing \S+ rather than adjacent over hypotenuse|inverted the ratio before applying", "inverts_trig_ratio"),
 (r"treated the (leg|adjacent leg) of \S+ as the hypotenuse|treating the given side as opposite|side adjacent to the \S+ angle, not the opposite|found the adjacent side correctly", "trig_side_roles_misassigned"),
 (r"read the numerator of the ratio as the literal side length", "ratio_numerator_as_side_length"),
 (r"inverse tangent instead of inverse sine|fed the ratio into", "inverse_trig_function_mismatched"),
 (r"treated \S+ as \S+ and halved|\S+ family", "special_angle_family_wrong"),
 (r"adding the legs|added inside the radical", "legs_combined_without_squaring"),
 (r"reported it as the hypotenuse", "hypotenuse_reported_for_leg"),
 (r"divided by the sine instead of multiplying|multiplied by \S+ instead of dividing|multiplied the hypotenuse by \S+ instead", "special_ratio_multiplied_not_divided"),
 (r"subtracted the two ratios|reported only \S+ and stopped|omitting the sine term", "omits_second_component"),
],
"GR.3.4":[
 (r"finds the (horizontal distance|side opposite|ramp surface length)|instead of the (vertical height|pole height|wall height)|uses the full hypotenuse|reads the \S+ pattern|ignores the ratio and reports", "adjacent_reported_for_opposite"),
 (r"halves the run|halves the side before applying|multiplies the (full )?hypotenuse by \S+ instead|applies the \S+ factor \S+ to half the hypotenuse|instead of taking half of it|applies the \S+ factor \S+ instead of the \S+ factor|uses the half-base|assumes the legs are equal", "special_factor_applied_to_wrong_part"),
 (r"forgets to subtract the initial height", "initial_value_not_subtracted"),
 (r"subtracts the angles first", "angles_combined_before_applying_ratio"),
 (r"adds the two (sides|distances)|subtracts the two (sides|distances)|adds all three (lengths|edge lengths)|adds the legs linearly|adding only the pole and the wire|subtracting linearly|adding linearly|doubles the side length", "legs_combined_without_squaring"),
 (r"multiplies the (sides|two distances)|computing the area", "perimeter_area_confusion"),
 (r"inverts the ratio", "inverts_trig_ratio"),
 (r"uses (sine|cosine) instead of tangent|uses sine instead", "wrong_trig_ratio_selected"),
 (r"multiplies instead of dividing", "special_ratio_multiplied_not_divided"),
 (r"omits the \S+ factor", "omits_fractional_factor"),
 (r"reports (it|the longer leg|the base) as the answer|but stops|finds only the base diagonal|halves the area and stops|reports the first runner's", "answers_intermediate_value"),
],
"GR.4.1":[
 (r"reverses both directions|subtracting from x and adding|adds to x correctly but adds to y|subtracts in both coordinates|adding to x and subtracting|handles x correctly but subtracts|adds to x instead of subtracting|only applies the translation|skips the reflection entirely", "translation_direction_reversed"),
 (r"reflects over the (y-axis|x-axis|line)|reflecting over the y-axis first|reflects over the line", "wrong_reflection_axis"),
 (r"applies (a|the) \S+ (rotation|clockwise|counterclockwise)|rotation rule|uses a \S+ rotation instead|negates both coordinates, treating the reflection as a \S+ (degree )?rotation", "wrong_rotation_rule"),
 (r"reverses the order", "composition_order_reversed"),
 (r"applies only the (rotation|dilation|reflection) and (stops|skips)", "composition_step_skipped"),
 (r"swaps the coordinates but forgets to negate|swaps the coordinates and negates the wrong one|negates only the (x|y)-coordinate", "coordinates_swapped_without_negating"),
 (r"adds the scale factor|multiplies only the x-coordinate|divides each coordinate by the scale factor", "inverts_conversion_direction"),
 (r"confuses rotation direction|sees a coordinate swap|sees negative coordinates", "transformation_misidentified_from_coordinates"),
],
"GR.4.2":[
 (r"moving a figure changes its size|turning a figure changes its size|flipping the figure|sliding a figure changes|believes flipping a figure changes its size|reversing orientation is a kind of size change|mislabels the reflection as a dilation|believes each rotation compounds", "rigid_motion_treated_as_dilation"),
 (r"treats(the)? ?the dilation as (a rigid motion|if it preserves congruence)|treats the dilation as|preserves congruence|scale factor of \S+ changes the size|can undo the size change|correctly identifies the dilation and the scale factor", "dilation_treated_as_congruence_preserving"),
 (r"false conditional|adds a false condition|only scale factors greater than|depends on how far|depends on the figure having symmetry|rather than from any rotation", "false_condition_invented_for_congruence"),
 (r"area ratio", "area_ratio_confused_with_linear_ratio"),
 (r"cannot match a rule|does not match|no single translation|chooses the wrong reflection axis|picks a single half-turn|sees the (negative x-coordinates|coordinates change|figure move)|assumes a size change and picks a dilation|picks a reflection alone|sees that the figure moved", "transformation_misidentified_from_coordinates"),
 (r"applying two transformations|stacking (transformations|two rotations)|applying more than one transformation|combining a (dilation|translation)|shrinking and then flipping|destroys the shape relationship|neither similar nor congruent", "composition_assumed_to_compound_size"),
 (r"reverses what a (dilation|similarity transformation) preserves", "dilation_treated_as_congruence_preserving"),
],
"GR.4.3":[
 (r"divides in the reverse order|from B to A|reverse order|divides by the scale factor instead of multiplying|applying it in the wrong direction", "scale_factor_direction_reversed"),
 (r"scales the (perimeter|area) by the (square|linear)|by the linear scale factor|doubles the scale factor instead of squaring|two-dimensional", "area_scaled_by_linear_factor"),
 (r"area ratio directly as the side ratio|halves the area ratio|squares the area ratio|adds the area ratio", "area_ratio_confused_with_linear_ratio"),
 (r"adds the scale factor|interprets the scale factor of \S+ as \S+ additional copies|counts how many times|subtracts the corresponding sides|adds the difference|copies the known corresponding side|adds it to the side", "adds_instead_of_scales"),
],
"GR.4.4":[
 (r"double-counts|doubles the count|tallying both clockwise", "symmetry_axes_double_counted"),
 (r"only the single (upright|vertical) axis|finds two of the three|overlooks the third|counts only the horizontal and vertical|finds only one of the two|no line symmetry at all|counts the diagonals as lines of symmetry|treating the equilateral triangle as if it were", "symmetry_axes_undercounted"),
 (r"confuses line symmetry with rotational|overlooks that (a rectangle|an equilateral triangle) (also )?has line symmetry", "line_vs_rotational_symmetry_confused"),
 (r"smallest angle of rotation|instead of the order|turned in \S+ steps", "rotation_angle_reported_for_order"),
 (r"point symmetry|half-turn map", "point_symmetry_wrongly_attributed"),
],
"GR.4.5":[
 (r"(does not|failing to) flip the sign|wrong signs|with their displayed signs|reads the \S+-coordinate of the center with the wrong sign|flips the sign on the \S+ term correctly but reads|flips the sign of the \S+ term but reads", "circle_center_signs_not_flipped"),
 (r"right side as the radius|radius directly instead of its square|uses the radius directly|places the radius on the right side|doubles the radius instead of squaring|divides the right side by \S+ instead of taking its square root", "radius_squared_confused_with_radius"),
 (r"full linear coefficient", "linear_coefficient_not_halved"),
 (r"adds the coordinate (differences|s) instead of using the distance formula|squares only the first coordinate", "distance_formula_replaced_by_sum"),
 (r"swaps the coordinates of the center|places the center value \S+ on the \S+ term", "coordinates_swapped"),
 (r"forgets to subtract the \S+ from|reads only \S+ from the center term|solves for the y-axis instead of the x-axis", "answers_intermediate_value"),
],
})

TOPIC_RULES.update({
# ------------------------------------------------------------------------ PR
"PR.1.1":[
 (r"threshold of '?at most|applies ≤|uses ≤ \S+ as the threshold|reading 'greater than", "threshold_boundary_error"),
 (r"number of distinct values|counts the number of non-Math categories|confusing distinct groups", "distinct_values_counted_not_occurrences"),
 (r"ascending order|misidentifies the beginning of the list|incorrectly confirms|looks at the end of the list", "ordering_violation_located_wrongly"),
 (r"over-counts|loses count|stops (scanning|after)|miscounts|misses the (final|second) |counts only|counts all repeated|identifies only \S+ as repeating|off-by-one|counts the total number of entries|adds \S+ to \S+ and then adds", "off_by_one_count"),
 (r"subtracts Blue from Green|adds Green and Red instead of subtracting", "subtracts_in_wrong_order"),
 (r"reads the Blue frequency", "reads_wrong_category"),
 (r"forgets to include the newly added", "off_by_one_count"),
],
"PR.1.2":[
 (r"misreads (Blue|the Green frequency|the Category D frequency)|misreads \S+ as \S+ instead of", "frequency_value_misread"),
 (r"swaps the (values|counts) for|reverses the counts for|miscounts Apple|counts Banana twice", "category_values_swapped"),
 (r"names the wrong source value|traces it to \S+'s value|invents a false equivalence", "error_traced_to_wrong_source"),
 (r"false structural rule|no categorical limit|line graphs are always valid|overstates the constraint|no required number of categories|axis-type restriction", "false_structural_rule_invented"),
 (r"applies the fix to the data instead of the headers|removes a necessary column|adds an unrelated column", "fix_applied_to_wrong_element"),
 (r"accepts the (recorded frequency|table|graph as accurate)|without (computing|verifying) the total|without verifying it against the list|sufficient evidence of accuracy", "total_accepted_without_verifying"),
 (r"finds the first occurrence|stops counting|applies a threshold condition", "threshold_boundary_error"),
 (r"omits the Yellow frequency", "single_cell_read_where_sum_needed"),
],
"PR.1.3":[
 (r"without (multiplying by the key|applying the key)|symbol counts directly without applying the key|as the final answer without applying the key", "pictograph_key_not_applied"),
 (r"reads the (Art|Math|Drama|Science) bar|reads (Wednesday|Monday|Cara|Ben)'s row|reads the Week \S+ data point|uses Wednesday's value|reads Tuesday correctly|misreads Tuesday's temperature|uses Wednesday and Thursday|misreads (Carlos|Ben)'s row", "reads_adjacent_value"),
 (r"selects (February|March|May|Silver|Red|White|Tuesday|Friday|Monday)|identifies White|the lowest count|the second-highest|placing it higher in the ranking|stopping the ranking", "reads_wrong_category"),
 (r"Week \S+ data point|wrong time point", "wrong_time_point_read"),
 (r"subtracts May from April|adds April and January|subtracts March from April|subtracts symbol counts directly", "subtracts_in_wrong_order"),
 (r"uses that value as the final answer instead of computing the difference", "answers_intermediate_value"),
 (r"accepts the claimed total", "total_accepted_without_verifying"),
 (r"checks only the first and last|stops at the first window|fails to (check|compare)|generalizes without verifying|incorrectly concludes", "misreads_direction_of_change"),
 (r"misidentifies the direction", "misreads_direction_of_change"),
 (r"using ≥ \S+ instead of strictly|counts only Cara", "threshold_boundary_error"),
 (r"miscalculates the average", "mean_divisor_miscounted"),
],
"PR.1.4":[
 (r"grand total as the denominator|uses the (grand total|Poor Food row total|Condition A Positive row total|total instrument players)|row total as the denominator|overall (failure|instrument-playing) rate|divides failed males by passed males|divides by all \S+ (people|students)", "row_total_used_for_column_total"),
 (r"reads only the (Female|Male|Freshman|Sophomore)|without adding the|sums (only )?the (Freshman|Apartment) row|adds the entire Apartment row|sums only (the first three quarters|Q\S+)|omits Q", "single_cell_read_where_sum_needed"),
 (r"which is the (lowest|third-lowest|fourth-lowest)|not the second-lowest|second-highest|selects (Monday|Thursday|Wednesday|Product|Org)", "rank_position_error"),
 (r"selects the wrong cell|as the numerator instead of", "unrelated_cells_compared"),
 (r"reads the (Nonfiction|Science Fiction|Fiction|Period|\S+th Grade)|correct (column but wrong row|row but wrong column)", "reads_adjacent_value"),
 (r"compares (Sales completions|the Owns Pet column)|two quantities from different columns|raw count rather than its rate|complement of the correct answer|proportion of sport players who do NOT", "unrelated_cells_compared"),
 (r"incorrectly (concludes|claims)", "total_accepted_without_verifying"),
 (r"subtracts Apartment No Pet", "subtracts_in_wrong_order"),
 (r"misreads Q", "frequency_value_misread"),
],
"PR.1.5":[
 (r"misclassifies the variable as categorical|misclassifies the count as categorical|values themselves are labels|has no numeric values|any variable in a study involving numbers|classifies the variable as continuous|binary categorical variable with a discrete", "categorical_numerical_misclassified"),
 (r"'?continuous'?|'?discrete'?|discreteness refers", "discrete_continuous_confused"),
 (r"'?nominal,?'?", "nominal_applied_to_measurement"),
 (r"scatter plot|histogram|line graph|pie chart|bar graph|pictograph|box plot|Plotting by month|Averaging across", "graph_type_mismatched_to_variable"),
 (r"false (structural limit|ordering (rule|requirement))|no maximum category count|absolute rule that does not exist|summation argument|axis orientation|do not require time intervals|overgeneralizes", "false_structural_rule_invented"),
 (r"confuses the number of bars|incorrectly generalizes that any frequency display|reverses the correct application|misidentifies sleep hours", "graph_type_mismatched_to_variable"),
],
"PR.2.1":[
 (r"divides by \S+ miscounting|treats the \S+ as 'no data'|dividing by \S+:", "mean_divisor_miscounted"),
 (r"unordered list|without sorting", "median_from_unsorted_list"),
 (r"lower of the two middle values|averaging the two middles", "two_middles_not_averaged"),
 (r"'no data'", "zero_dropped_as_no_data"),
 (r"averages the old mean with the new value", "new_mean_from_averaging_means"),
 (r"divides the new total by the old count|divides the new total by the original count|forgetting that n grew|forgetting the set shrank|keeps the count at", "new_total_divided_by_old_count"),
 (r"resist outliers|dragged toward the outlier|assumes all statistics respond equally|treats the range as a measure of center", "outlier_resistance_misjudged"),
 (r"reports the (median|mode|mean|range)|computes the (mean|range)|picks the largest value|reading 'most' as 'greatest'|the middle value of the unordered", "reports_wrong_center_measure"),
 (r"averages only the smallest and largest|adds the largest and smallest|median of the three distinct scores|miscounts the middle position|off by one position|fourth value", "two_middles_not_averaged"),
 (r"drops the negative sign|picks \S+ as the minimum", "drops_negative_sign"),
 (r"maximum value", "range_from_single_extreme"),
 (r"subtracts \S+ three times|forgetting the mode appears twice|reports the full total|assumes the removed value equals|pairs each mean with the wrong count|forgets to subtract the removed value|assumes (adding|removing) a single value", "answers_intermediate_value"),
],
"PR.2.2":[
 (r"simple unweighted mean|ignoring the (weights|ratio|credit weights)|unweighted (mean|equation)|averages the (two|three) (section averages|distinct)|ignoring how many|averaging", "weights_ignored"),
 (r"swaps (two of the three weights|the weights|the two weights|the section sizes|the two counts)|fully reverses the weights|reverses the (ratio|credit weights)|applies the weights to the wrong categories|keeps the weights but swaps|mismatches the counts", "weights_swapped"),
 (r"matches the largest weight to the largest score|because it is the larger number|assuming the bigger group|most common (point value|rating)|weighting tests .{0,40}more must raise|weights exams .{0,30}more must win", "largest_weight_matched_to_largest_score"),
 (r"never finds the missing weight|reuses the lab weight|applies the exam weight \S+ to the final|finds the project's weight|drops the \S+-credit course", "missing_weight_reused"),
 (r"forgets to divide by the final's weight|divides the grade-to-project gap|assumes equal weighting", "target_weight_not_divided_out"),
 (r"uses only the test average|uses the larger section's average", "single_stage_reported"),
 (r"attaches it to the wrong scheme|reports the raw gap|identifies Plan B as higher", "reads_wrong_category"),
],
"PR.2.3":[
 (r"multiplies the mean by (the count of known values|\S+ known|one more than the count)|divides the four-day total by", "total_from_wrong_count"),
 (r"assumes the missing (number|value|score|game) (simply )?equals the mean|assumes the fifth day equals|assumes y equals the mean|sets both unknowns to|leaves the other at the mean|leaves the other two at the mean", "missing_value_assumed_equal_to_mean"),
 (r"adds the range to the (largest|current maximum)|instead of the (minimum|true minimum)|applies the range to it", "range_added_to_wrong_extreme"),
 (r"range of only the known values|uses \S+ as the maximum instead", "range_from_known_values_only"),
 (r"subtracts the mean from the sum", "mean_subtracted_from_sum"),
 (r"maximizes only one|reports the cap value|splits the remaining sum equally|swaps the labels|finds the other valid value", "optimisation_bound_misused"),
 (r"reports the range|averages the (four|three|five|two) known", "reports_wrong_center_measure"),
 (r"uses the range itself|divides the total by \S+ instead of", "total_from_wrong_count"),
],
"PR.2.4":[
 (r"mean, which the \$?\S+ (mansion|salary) inflates|outlier-sensitive mean|dismissing the one large value|overlooks that R's value|because of its value|prefers the mean because it uses all values|includes the outlier through the mean|pulls the mean down|low outlier", "outlier_effect_on_mean_dismissed"),
 (r"larger range as a strength|wider range as flexibility|large range as evidence of strong performance|treats a higher mean as better", "larger_range_read_as_better"),
 (r"skew|mean above the median|rules out symmetry|identifies a skew but the wrong direction", "skew_direction_misread"),
 (r"equates equal means with equal consistency|equal means with identical performance|higher-scoring student must also be more consistent|wrongly links a higher mean to lower consistency|conflating center with spread|assumes equal (means|medians) because|assumes the means are equal|equal centers as equal spread|assumes equal ranges|assumes equal medians", "equal_means_assumed_equal_consistency"),
 (r"equal medians imply equal means", "equal_medians_assumed_equal_means"),
 (r"reports (its|the) maximum value \S* ?as the range|uses its maximum value", "range_from_single_extreme"),
 (r"even, steady pattern as more spread|even spacing|focuses only on the (lower mean|small gap)|ignores both the|fixates on|attends only to the small gap|focuses only on a single high game|confuses the range, a spread measure|draws a conclusion about spread", "center_spread_confusion"),
 (r"takes the largest value of Dataset A|reads Student A as higher-scoring|reverses the means|misreads which neighborhood|but concludes (A|Dataset A) is (greater|more)|selects B as greater|miscounts the middle|selects Dataset A correctly but|compares the first four values|states the wrong direction", "reads_wrong_category"),
 (r"reports the mean's value|reports the median's value|mixing up the two measures", "reports_wrong_center_measure"),
],
"PR.2.5":[
 (r"reads Q\S* ?(the (left|right) edge)|as the median|reads the median \S* ?as Q|reads the maximum|reads Q\S+\)? instead of Q|confusing the two quartiles", "quartile_read_as_median"),
 (r"using the median in place of Q", "iqr_computed_using_median"),
 (r"full range|max minus min|instead of the IQR", "range_from_single_extreme"),
 (r"whisker length|long whisker", "whisker_length_as_typical_value"),
 (r"equal endpoints|shared range", "equal_endpoints_read_as_equal_distributions"),
 (r"midpoint of the minimum and maximum", "reports_wrong_center_measure"),
 (r"single highest value|uses the medians to judge spread|assumes uniform spread|misreads the plot", "center_spread_confusion"),
],
"PR.3.1":[
 (r"compares (red to non-red|black to non-black|black to white|the \S+ not-red marbles)|as a ratio instead|divides the \S+ favorable (faces|cards) by the \S+ non-favorable|reading '\S+ black' as", "favourable_over_unfavourable"),
 (r"miscounts the total|dropping (the red pen|one marble)|by starting the count at|the total as \S+ pens", "outcome_total_miscounted"),
 (r"double-counting|adds the \S+ odd outcomes and", "compound_outcomes_double_counted"),
 (r"reports the total instead|solves for the total number|reports that instead of the class size|multiplies the \S+ (blue|green) marbles|adds only one fifth|treating (the \S+ blue|yellow) as the total", "proportion_solved_for_wrong_unknown"),
 (r"adds the \S+ new marbles|removes the \S+ (green|from the total)|ignores the (added|removed) marbles|forgets they also raise|forgets the total also shrank|forgets they were red|forgets the green count dropped", "added_items_not_reflected_in_total"),
 (r"includes \S+ itself, reading|counts only \S+ and \S+ as greater than|as '\S+ or greater'", "threshold_boundary_error"),
 (r"probability of NOT|finds P\(not|finds P\(white\)|P\(red\) = \S+ out of \S+ instead of P\(not red\)|instead of P\(black\)", "reports_event_not_complement"),
 (r"counts only (red|yellow|blue)|forgetting to add|counts only the (odd|'greater)|ignoring the", "omits_second_component"),
 (r"divides the \S+ red sections by the number of colors|uses the value \S+ from the phrase|by \S+ \(the threshold value\)|divides the \S+ favorable cards by", "uses_wrong_total"),
 (r"counts \S+ as a prime|drops \S+ from the (prime list|list of multiples)|counts multiples of \S+ instead|counts the factors of", "event_set_miscounted"),
 (r"finds the \S+ favorable marbles but miscounts|finds \S+ primes but miscounts", "outcome_total_miscounted"),
],
"PR.3.2":[
 (r"complement is always an even chance|complement of any event is an even chance", "complement_assumed_half"),
 (r"subtracts the (event probability|defective rate) twice", "event_probability_subtracted_twice"),
 (r"complement of only the (tea|coffee)|adds the tea and coffee preferences|excludes only the four multiples", "partial_complement_taken"),
 (r"counts \S+ as (a multiple|prime)|drops \S+ from the list of multiples|counts \S+ twice because", "event_set_miscounted"),
 (r"the event itself|reports the probability (that|of)", "reports_event_not_complement"),
 (r"forgets to subtract it from|but forgets to subtract", "reports_event_not_complement"),
 (r"forms a ratio of", "favourable_over_unfavourable"),
 (r"adds the two (single-flip|single-roll|single-spin) (head )?probabilities|converts to rain probabilities|multiplies the two rain probabilities", "adds_probabilities_instead_of_multiplying"),
 (r"treats 'at least one|reads 'at least", "threshold_boundary_error"),
 (r"correctly finds the total membership|reports the given non-senior count|divides the non-senior count", "proportion_solved_for_wrong_unknown"),
],
"PR.3.3":[
 (r"reports only (the first draw's|Spinner|the coin|the die|the spinner|the card)|reports the probability of a single red draw|drops the coin event entirely|ignores the (die event|coin|spinner|card)|treats '?a \S+ on both rolls'? as a single event", "single_stage_reported"),
 (r"with replacement|without replacement", "replacement_status_wrong"),
 (r"keeps the total at|forgets the total (also )?drops|reduces the (red|winner|blue) count .{0,40}(but|keeps)|wrongly lowers the yellow count", "total_not_reduced_between_draws"),
 (r"adds the (probabilities|two probabilities|two single-roll probabilities|coin probability)|instead of multiplying", "adds_probabilities_instead_of_multiplying"),
 (r"adds the numerators over the product", "numerators_added_over_product"),
 (r"stops after (two draws|the first two draws)", "sequence_stopped_early"),
 (r"without taking the complement|instead of the complement of both red", "reports_event_not_complement"),
 (r"miscounts green", "outcome_total_miscounted"),
 (r"cube root|multiplies the compound probability|divides the compound probability|reports the compound probability", "proportion_solved_for_wrong_unknown"),
],
"PR.3.4":[
 (r"divides by (the whole class|all \S+)|instead of the \S+ (sport players|red ones|dog owners|adults|seniors|soccer fans|members|people)|divides by all \S+ students|uses the grand total", "whole_population_as_denominator"),
 (r"reverses the condition|reversed conditional|conditions on (basketball|having passed|taking math only)|probability that a man owns a car", "conditional_reversed"),
 (r"reports the joint probability|joint probability of taking both|without dividing by", "joint_reported_as_conditional"),
 (r"reports the probability of (playing a sport|drawing red|owning a dog|being (a senior|an adult|a man)|preferring coffee)|ignores the condition|reports the prior probability|starting probability of red|ignoring that the student passed|ignoring the (band|star|cat|marked|officer|sugar|car-owner) (information|condition)", "prior_reported_ignoring_condition"),
 (r"uses the \S+ (sport players who are not|dog owners without|seniors who are not|basketball-only)|forms a ratio of (star|marked) reds", "favourable_over_unfavourable"),
 (r"lowers the (total|red count)|wrongly lowers the red count|treats the draws as with replacement|forgets the total drops", "total_not_reduced_between_draws"),
 (r"subtracts the two percentages|adds the two percentages", "adds_probabilities_instead_of_multiplying"),
 (r"forgets to subtract the overlap", "overlap_not_subtracted"),
 (r"computes the probability that the second marble is blue", "reads_wrong_category"),
],
"PR.3.5":[
 (r"finds the union|reports the size of the intersection|confusing the two operations|only in (A|B) and not in", "union_intersection_swapped"),
 (r"reports set A itself|complement is empty|reports the entire universal set", "complement_misidentified"),
 (r"without subtracting the (shared elements|overlap)|adds the two (set sizes|groups)|adds the three subject totals|forgets to add back", "overlap_not_subtracted"),
 (r"subtracts the overlap twice|adds the overlap instead of subtracting|subtracts the triple overlap instead", "overlap_subtracted_twice"),
 (r"uses only the \S+ students in both|subtracts the tea group|people who like neither|takes the difference of the two set sizes", "union_intersection_swapped"),
],
"PR.4.1":[
 (r"reverses (the meaning|the direction)|misreads the direction|defaults to 'positive'|confuses the sign of the data values", "misreads_direction_of_change"),
 (r"strength|weak|tight(ly)? (relationship|clustering)|clustering|steepness|visual gentleness|number of points|spread of x-values|central location|positive associations are inherently stronger|negative direction with a weak", "strength_confused_with_direction"),
 (r"imperfect|any scatter|no association exists|points must fall exactly|not a straight line, no association|denies", "scatter_read_as_no_association"),
 (r"curv|U\b|rising right half|falling left half|linear relationship can change direction|labels any upward trend 'linear'|flattening", "curved_pattern_forced_linear"),
 (r"outlier|single point|contrary point|two extreme points|leverage points|two higher points|a few (high-high )?points", "outlier_influence_misjudged"),
 (r"constant difference|equal decrements|correlation coefficient|five points are too few|plotted image is required|a graph is required", "false_precondition_for_association"),
 (r"over-reads random scatter", "outlier_influence_misjudged"),
],
"PR.4.2":[
 (r"adds the x-values in the denominator", "slope_denominator_summed"),
 (r"adds the y-values in the numerator", "slope_numerator_summed"),
 (r"larger intercept improves fit|judges fit by slope alone|fit cannot be assessed", "fit_judged_by_intercept"),
 (r"inverts the (ratio|slope)|run over rise", "slope_run_over_rise"),
 (r"swaps slope and intercept|swaps the roles|confuses the slope with the intercept|treats the intercept as a per-minute rate|misreads the (slope|intercept)|selects the (y-intercept|slope) \(\S+\) instead", "slope_intercept_swap"),
 (r"adds the slope and intercept|adds all three numbers|adds before multiplying|treats the slope as a fixed total|subtracts the intercept from the slope|divides \S+ by \S+ \(the slope's absolute value\)|reads the intercept as the entire bill", "flat_fee_merged_into_rate"),
 (r"forgets to add the intercept|omits the intercept of|ignores the intercept", "omits_constant_term"),
 (r"names the variable x", "variable_term_read_as_rate"),
 (r"drops (its|the) negative sign|flips the sign of the intercept|computes the magnitude correctly but drops", "drops_negative_sign"),
 (r"subtracts the intercept but forgets to divide|stopping at|divides by the slope but ignores|adds the intercept instead of subtracting", "answers_intermediate_value"),
],
"PR.4.3":[
 (r"by the new value instead of the original|from the new price|increases the new price by another|applying the change forward|uses the higher (final price|starting price) as the basis", "percent_change_wrong_base"),
 (r"absolute (change|reduction)", "absolute_change_as_percent"),
 (r"new value as a percent of the original|final value as a percent of the original|remaining price as a percent", "new_over_original_as_change"),
 (r"labels the direction backward|reverses the direction|confusing an increase with a decrease", "misreads_direction_of_change"),
 (r"misplaces the decimal", "percent_decimal_overshift"),
 (r"naively adds the percentages|adds the magnitudes of the two changes|adds the two rates|ignoring that the second", "percent_changes_added"),
 (r"compares absolute (dollar changes|dollar drops)|assumes (the increases must be equal|smaller starting values)", "percent_change_wrong_base"),
 (r"counts only the second year's growth", "single_period_counted"),
],
"PR.4.4":[
 (r"reverses the direction into an implausible causal|asserts a causal direction \(scores cause libraries\)|asserts the reverse causal direction|proposes an implausible reverse causal|fixes only the direction", "reverse_causation_asserted"),
 (r"confound|ignoring the confounder|larger feet improve reading|shoe size as a useful predictor|selection bias|Comparing years without controlling|observational design", "confounder_ignored"),
 (r"average tendency|overgeneralizes a tendency|turns an observed association into a causal|converts (a tendency|an association) into|treats the mean as if every|assumes the mean implies|confuses the mean with the maximum|turns past frequency|absolute claim|value judgment|treats one associated factor|treats an observed association as proof|asserts that exercise causes|accepts a direct causal claim|accepts the spurious causal claim|accepts the overgeneralization|denies (the|any)", "tendency_stated_as_absolute"),
 (r"in-range fit guarantees|claims false precision|over-rejects (all|linear)|justifies extrapolation|assumes the observed trend continues|ignores the model's data range|raises an irrelevant objection about the amount of study|interpolation within the data range", "model_fit_assumed_to_transfer"),
 (r"sample size alone|clear majority within the sample|generalizes a \S+-day sample|unsupported absolute claim about a season|representativeness|over-rejects all (surveys|polling)|irrelevant objection about the size of the majority", "overgeneralizes_from_sample"),
],
})

STRAND_RULES = {}
GLOBAL_RULES = []

# ---------------------------------------------------------------------------
# Second pass: rules for distractors the first pass left unmatched. Appended
# (lower priority) so they cannot shadow a first-pass decision.
EXTRA = {
"AR.1.1":[(r"treats −\S+\(−\S+\) as", "sign_error_on_constant")],
"AR.2.2":[(r"adds \\?\$?\S+ to \\?\$?\S+ instead of subtracting", "adds_instead_of_subtracts")],
"AR.2.4":[(r"same addition error", "constants_added_to_solve")],
"AR.2.6":[(r"computes Δx = .{0,20}and reports it as the slope", "delta_x_used_as_slope"),
          (r"sees the y-intercept .{0,20}and reports", "slope_intercept_swap")],
"AR.2.7":[(r"takes the reciprocal .{0,60}forgets to negate", "reciprocal_without_negation")],
"AR.2.8":[(r"treating all factors as additive terms", "wrong_inverse_operation_chosen")],
"AR.3.4":[(r"discriminant as", "discriminant_miscomputed"),
          (r"divides by \$a = \S+\$ instead of", "quadratic_formula_denominator_omitted")],
"AR.3.5":[(r"axis of symmetry .{0,30}reports it as the minimum", "axis_reported_as_extreme_value")],
"AR.3.6":[(r"reverses both shift directions", "transformation_shift_direction_reversed")],
"AR.4.1":[(r"treats the lone .{0,20}as", "drops_negative_sign")],
"AR.4.2":[(r"failing to add exponents", "adds_exponents_wrongly")],
"AR.4.4":[(r"adds the (denominator's|variable) exponents?", "adds_exponents_wrongly")],
"AR.4.5":[(r"distributes the \S+ incompletely", "partial_distribution")],
"AR.4.8":[(r"moves the \S+ outside the radical", "perfect_square_moved_out_unrooted")],
"AR.4.9":[(r"but does not square, reporting", "answers_intermediate_value")],
"AR.4.10":[(r"evaluates \$\S+\^\{\S+\}\$ as \S+ \\times", "base_times_exponent"),
           (r"raises the inner exponent to the outer", "inner_exponent_raised_not_multiplied"),
           (r"inner power as base times exponent", "base_times_exponent"),
           (r"adds the denominator's exponent", "adds_exponents_wrongly")],
"AR.4.12":[(r"grows the final amount forward instead of reversing", "percent_applied_forward_not_reversed")],
"GR.2.1":[(r"adds only the two equal sides", "side_omitted_from_perimeter"),
          (r"report(s|ing) only l \+ w", "answers_intermediate_value"),
          (r"evaluates \S+x . \S+ as \S+x only", "partial_distribution")],
"GR.2.2":[(r"us(es|ing) C = πr instead", "factor_of_two_omitted")],
"GR.2.3":[(r"uses the diameter .{0,20}instead of the radius", "radius_diameter_substituted")],
"GR.2.4":[(r"forgets to divide by \S+ at the final step|without multiplying by \S+ first", "one_of_two_divisors_omitted"),
          (r"multiplies area by base instead of dividing|divides the area by \S+ instead of taking the square root|divides by \S+ instead of \S+:|divides \S+x = \S+ by|rounds \S+ . \S+ = \S+ to", "wrong_inverse_operation_chosen"),
          (r"number of sides of a square|half the perimeter rather than a sixth", "wrong_side_count_for_polygon")],
"GR.2.5":[(r"computes volume", "volume_surface_area_confusion"),
          (r"computes one face area only", "opposite_faces_not_doubled"),
          (r"divides by \S+ . \S+ = \S+ instead of|divides by \S+πr² = \S+ instead of", "wrong_inverse_operation_chosen"),
          (r"ignores the cylinder entirely", "omits_second_component"),
          (r"reporting r² as the radius", "forgets_square_root")],
"GR.2.6":[(r"applies the ½ factor twice", "applies_fractional_factor_twice"),
          (r"forgets to multiply by the prism length", "omits_second_component")],
"GR.2.7":[(r"adding an extra \S+ to the constant", "phantom_term_introduced"),
          (r"uses s instead of s²", "radius_not_squared")],
"GR.3.1":[(r"computes the area of the rectangle instead of the diagonal", "perimeter_area_confusion")],
"GR.3.2":[(r"sqrt\{2\}", "special_triangle_factor_swapped")],
"GR.3.3":[(r"instead of (sine|cosine|tangent)", "wrong_trig_ratio_selected")],
"GR.3.4":[(r"reports the longer leg", "short_long_leg_roles_confused"),
          (r"adds the height linearly", "legs_combined_without_squaring"),
          (r"altitude with the 45-45-90 factor", "special_triangle_factor_swapped"),
          (r"reports the base \S+ \+ \S+ = \S+ instead", "answers_intermediate_value")],
"GR.4.1":[(r"negates both coordinates .{0,30}treating the reflection", "wrong_rotation_rule"),
          (r"reversing the order", "composition_order_reversed")],
"GR.4.2":[(r"comparing original to image instead of image to original", "scale_factor_direction_reversed")],
"GR.4.5":[(r"adds the coordinates instead of using the distance formula", "distance_formula_replaced_by_sum"),
          (r"reads only \$\S+ = \S+\$ from the center term", "answers_intermediate_value")],
"PR.1.1":[(r"confusing the frequency of one value with the count of repeated", "distinct_values_counted_not_occurrences"),
          (r"adds the \S+ new values to the existing count", "off_by_one_count")],
"PR.1.4":[(r"adds the Male Chess cell", "unrelated_cells_compared")],
"PR.2.1":[(r"removing one value leaves the mean unchanged", "answers_intermediate_value")],
"PR.2.3":[(r"splits the remaining sum", "optimisation_bound_misused")],
"PR.2.4":[(r"judges by the largest values in Dataset", "reads_wrong_category"),
          (r"focuses on a single high game|dismisses the spread difference", "center_spread_confusion")],
"PR.3.1":[(r"counts only one favorable face", "omits_second_component"),
          (r"reads '\S+ black' as", "uses_wrong_total"),
          (r"never divides by the numerator", "proportion_solved_for_wrong_unknown")],
"PR.3.2":[(r"as the probability of red on a single spin", "single_stage_reported")],
"PR.3.4":[(r"by all \d+ people", "whole_population_as_denominator")],
"PR.4.1":[(r"a few higher points", "outlier_influence_misjudged")],
"QR.1.1":[(r"less than a nearby decimal", "irrational_assumed_larger"),
          (r"breaking the near-tie", "closer_endpoint_error"),
          (r"confusing the interval", "wrong_perfect_square_bracket")],
"QR.1.2":[(r"misconverts?|confusing \$?\\?frac|swaps the (top|bottom) two", "fraction_digit_gluing"),
          (r"misreads \S+ as greater than", "place_value_slip")],
"QR.1.3":[(r"the decimal repeats", "terminating_test_confusion"),
          (r"uses \$?\\?frac\{1\}\{4\}\$? = \S+ instead of", "reads_wrong_category")],
"QR.1.4":[(r"but selects \S+ itself|down to \S+ and .{0,30}up to \S+, then subtracts", "rounds_to_bounding_integer"),
          (r"all the way up to|rounds \S+ up to \S+ rather than down", "closer_endpoint_error")],
"QR.1.5":[(r"drops the \S+√\S+ term", "partial_distribution")],
"QR.2.1":[(r"identifies the blue part|selecting (green|red|yellow) instead", "scales_by_wrong_ratio_term"),
          (r"rounds up to \S+ instead of taking the", "off_by_one_count")],
"QR.2.2":[(r"reports it as the savings", "answers_intermediate_value"),
          (r"divides Train A's speed by the speed difference", "relative_speed_ignored")],
"QR.2.3":[(r"ignoring the prior markup", "percent_applied_to_wrong_stage_base")],
"QR.2.4":[(r"subtracts \S+ directly from \S+ .{0,20}instead of dividing", "percent_sign_confusion"),
          (r"adds \S+% of .{0,30}(to the total|to the failing count)", "percent_applied_forward_not_reversed"),
          (r"subtracts only the first markup percent", "percent_changes_added")],
"QR.2.5":[(r"reports Brand A's unit rate", "reads_wrong_category")],
"QR.2.6":[(r"uses \S+ min as denominator", "wrong_period_count_used")],
"QR.3.1":[(r"pulling it into the numerator", "drops_grouping_symbols"),
          (r"confuses '\S+ less than'|confuses '\S+ more than'", "keyword_mistranslated"),
          (r"reversing the subtraction order", "less_than_order_reversed")],
"QR.3.2":[(r"inverts the relationship", "slope_run_over_rise"),
          (r"finds the starting value", "constant_read_as_rate")],
"QR.3.5":[(r"ignoring the minus before", "drops_negative_sign"),
          (r"as \$?[-+]?\S+x?\$? instead of \$?[-+]", "sign_error_on_constant"),
          (r"brings the \$?-?\S+\$? down unmultiplied", "partial_distribution")],
"QR.3.7":[(r"reaches \$.{0,20}\$ but reports \S+ without dividing", "answers_intermediate_value"),
          (r"computes g's rate by inverting", "slope_run_over_rise")],
"QR.3.8":[(r"forgets to apply the \S+ to the x term", "partial_distribution"),
          (r"forgets to add the \$?\S+x?\$? term outside", "answers_intermediate_value")],
"QR.4.1":[(r"cross-multiplies to get", "answers_intermediate_value"),
          (r"computes only the cyclist's distance", "answers_intermediate_value"),
          (r"ignoring the tier|using only the discounted tier", "omits_second_component"),
          (r"applies a \S+% increase to the current value", "percent_applied_forward_not_reversed")],
"QR.4.2":[(r"omits (the|its) \\?\$?\S+ (registration|delivery|flat) fee", "omits_constant_term"),
          (r"adds the deposit and spending instead of subtracting", "adds_instead_of_subtracts")],
"QR.4.3":[(r"difference in starting amounts by \S+'s rate alone", "rates_summed_instead_of_differenced")],
}
for _t,_r in EXTRA.items(): TOPIC_RULES.setdefault(_t,[]).extend(_r)

# ---------------------------------------------------------------------------
# Third pass: PRIORITY rules, prepended so they win over the broader rules
# above. Each one fixes a misassignment found by reviewing the output -- a
# slug that should have fired but was shadowed by a more general pattern.
PRIORITY = {
"AR.1.2":[(r"one-to-one \(injective\)|all outputs must be distinct|injective function", "injectivity_used_as_function_test")],
"AR.1.4":[(r"Second differences are constant at", "zero_second_differences_read_as_quadratic")],
"AR.2.1":[(r"misinterprets the resulting contradiction", "contradiction_read_as_solution")],
"AR.2.3":[(r"before multiplying", "input_added_to_fee_before_multiplying")],
"AR.2.4":[(r"every point on the shared line", "one_point_reported_for_infinite_solutions")],
"AR.2.5":[(r"unwarranted domain restriction", "unwarranted_domain_restriction_added")],
"AR.4.6":[(r"identical denominators", "identical_denominators_multiplied")],
"PR.1.3":[(r"reads the Week \S+ data point", "wrong_time_point_read")],
"PR.2.1":[(r"as 'no data'", "zero_dropped_as_no_data")],
"PR.2.4":[(r"equal medians imply equal means", "equal_medians_assumed_equal_means")],
"PR.3.3":[(r"adds the numerators over the product", "numerators_added_over_product")],
"PR.3.5":[(r"forgets to add back", "triple_overlap_not_added_back")],
"PR.4.1":[(r"gentleness of the slope|steepness", "steepness_confused_with_strength")],
"PR.4.4":[
  (r"may differ from those who do not|Interviewing only students|Comparing years without controlling|observational design", "study_design_confused_with_causal_evidence"),
  (r"(turns|converts) an (observed )?association into a causal|accepts the spurious causal claim|treats an observed association as proof", "causation_from_association"),
  (r"ignores the model's data range|justifies extrapolation|assumes the observed trend continues|irrelevant objection about the amount of study", "extrapolates_beyond_data"),
  (r"over-rejects", "over_rejects_valid_model"),
],
"QR.1.1":[(r"dropping the radical", "radical_dropped_for_coefficient")],
"QR.3.1":[(r"incorrectly grouping|groups the \S+ with the tripling|pulls the \S+ inside the average", "wrong_quantity_grouped")],
"QR.3.7":[(r"stays ahead", "larger_start_assumed_to_stay_ahead")],
"QR.4.1":[(r"computes the discount amount", "reports_discount_not_price")],
}
for _t,_r in PRIORITY.items(): TOPIC_RULES[_t] = _r + TOPIC_RULES.get(_t,[])

# Fourth pass: corrections from spot-checking sampled assignments.
CORRECTIONS = {
"QR.4.2":[(r"divides the total revenue by the drink price", "reads_wrong_category")],
"GR.3.1":[(r"reports \S+ as the area, forgetting to take the square root", "forgets_square_root")],
"AR.1.3":[(r"reads the range endpoints directly from the domain boundaries", "range_read_from_domain_endpoints")],
"QR.3.7":[(r"similar-looking fractions", "linearity_assumed_to_imply_equal_rates")],
"GR.2.4":[(r"reports that result as x without subtracting", "answers_intermediate_value")],
}
for _t,_r in CORRECTIONS.items(): TOPIC_RULES[_t] = _r + TOPIC_RULES.get(_t,[])


# Phase 3 closeout. GR_A_034.B carries two entangled errors; its answer-choice
# value (301.44) is pi*r^2*h for the cone, so the arithmetic it verifies is the
# missing one-third, not the omitted cylinder. Tagged to the error the number
# proves; the item itself is logged in content-fixes-needed.md as an authoring
# defect, since a distractor must trace to exactly one coherent error.
TOPIC_RULES["GR.2.6"] = [
 (r"cone's contribution without the ⅓ factor", "omits_fractional_factor"),
] + TOPIC_RULES["GR.2.6"]

# Phase 3 closeout, item 2: four approved one-off slugs replacing assignments
# that were forced into an existing slug during tagging.
CLOSEOUT = {
"QR.1.3":[(r"uses \$?\\?frac\{1\}\{4\}\$? = \S+ instead of", "complement_used_instead_of_value")],
"QR.2.1":[(r"rounds up to \S+ instead of taking the", "rounds_up_where_floor_required")],
"PR.1.5":[(r"Averaging across \S+ years smooths away", "averaging_removes_the_variation_studied")],
"PR.3.5":[(r"like neither .{0,30}mislabels it as", "neither_reported_as_both")],
}
for _t,_r in CLOSEOUT.items(): TOPIC_RULES[_t] = _r + TOPIC_RULES.get(_t,[])
